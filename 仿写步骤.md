# FOC 电机控制程序仿写步骤

## 前置条件：CubeMX 底层外设配置

以下外设必须在 CubeMX 中配置好并生成代码：

| 外设 | 核心配置 | 用途 |
|------|---------|------|
| **TIM1** | 中央对齐模式1, ARR=4249, RepetitionCounter=3, 三相互补PWM, 死区85, TRGO2=Update | 20kHz PWM + 触发ADC1 |
| **ADC1** | 注入组3通道(PA0/PA2/PA3), TIM1_TRGO2触发, 采样24.5Cycles | 三相电流10kHz同步采样 |
| **ADC2** | 常规组2通道(PB2/PB11), TIM6_TRGO触发, 采样640.5Cycles, DMA循环 | 母线电压+温度1kHz采样 |
| **TIM6** | PSC=169, ARR=999, TRGO=Update, 中断使能 | 1kHz时基+触发ADC2 |
| **SPI1** | 主机模式, 16bit, CPOL=High/CPHA=2Edge, /8分频, 软件NSS | MA600A编码器通信 |
| **DMA1_CH1** | ADC2, P→M, 半字对齐, Circular | ADC2数据自动搬运 |
| **GPIO** | PA4(ENC_CS推挽高), PB14(N_SLEEP推挽低), PB15(N_FAULT输入上拉) | 编码器片选+驱动使能+故障检测 |
| **USART1** | 115200, 8N1, DMA1_CH2(TX) | CLI调试串口(可选) |

> **中断优先级必须正确**：ADC1_2_IRQn=0, SPI1_IRQn=1, TIM6_DAC_IRQn=2, DMA1_CH1/CH2=3, USART1=4

---

## 第1步：最小框架搭建

### 1.1 创建 `App/program.h`

```c
#ifndef PROGRAM_H
#define PROGRAM_H

#include "main.h"
#include "adc.h"
#include "tim.h"
#include "gpio.h"

void program_init(void);
void program_task(void);

#endif
```

### 1.2 创建 `App/program.c` — 最小骨架

```c
#include "program.h"

/* ── 常量 ── */
#define ADC2_DMA_LEN        2
#define ADC_REF_V           3.30f
#define ADC_FULL_SCALE      4095.0f
#define VBUS_R_UP           240000.0f
#define VBUS_R_DOWN         10000.0f
#define PI                  3.14159265359f
#define TWO_PI              6.28318530718f

/* ── 全局变量 ── */
static volatile uint16_t g_adc2_dma_buf[ADC2_DMA_LEN];
static volatile uint32_t g_tim6_tick_ms = 0;

/* ── 工具函数：限幅 ── */
static float program_clamp_f32(float val, float lo, float hi)
{
    if (val < lo) return lo;
    if (val > hi) return hi;
    return val;
}

/* ── 工具函数：角度归一化到 [0, 2π) ── */
static float program_wrap_angle_0_2pi(float angle)
{
    if (!isfinite(angle)) return 0.0f;
    float turns = floorf(angle / TWO_PI);
    angle -= turns * TWO_PI;
    if (angle < 0.0f) angle += TWO_PI;
    return angle;
}

/* ── 工具函数：角度差归一化到 [-π, π) ── */
static float program_wrap_delta_pm_pi(float angle)
{
    if (!isfinite(angle)) return 0.0f;
    return program_wrap_angle_0_2pi(angle + PI) - PI;
}

/* ── 启动 ADC2 DMA + TIM6 链 ── */
static void program_start_adc2_dma_chain(void)
{
    HAL_ADC_Start_DMA(&hadc2, (uint32_t *)g_adc2_dma_buf, ADC2_DMA_LEN);
    HAL_TIM_Base_Start_IT(&htim6);
}

/* ── 系统初始化 ── */
void program_init(void)
{
    /* 功率级上电保持休眠 */
    HAL_GPIO_WritePin(N_SLEEP_GPIO_Port, N_SLEEP_Pin, GPIO_PIN_RESET);

    /* ADC 硬件校准 */
    HAL_ADCEx_Calibration_Start(&hadc1, ADC_SINGLE_ENDED);
    HAL_ADCEx_Calibration_Start(&hadc2, ADC_SINGLE_ENDED);

    /* 启动慢采样链 */
    program_start_adc2_dma_chain();
}

/* ── 后台主任务（1kHz 轮询） ── */
void program_task(void)
{
    static uint32_t last_ms = 0;
    uint32_t now_ms = g_tim6_tick_ms;
    if (now_ms == last_ms) return;
    last_ms = now_ms;

    /* 后续步骤在此添加逻辑 */
}

/* ── TIM6 1ms 时基回调 ── */
void HAL_TIM_PeriodElapsedCallback(TIM_HandleTypeDef *htim)
{
    if (htim->Instance == TIM6) {
        g_tim6_tick_ms++;
    }
}
```

### 1.3 修改 `Core/Src/main.c` 对接

```c
/* 在 USER CODE BEGIN Includes 中添加 */
#include "program.h"

/* 在 USER CODE BEGIN 2 中添加（MX_xxx_Init 全部调用之后） */
program_init();

/* 在 USER CODE BEGIN 3 中（while(1) 循环体内） */
program_task();
```

> **验证点**：编译通过，下载运行，`g_tim6_tick_ms` 在调试器中每秒递增 1000。

---

## 第2步：ADC2 读数验证（验证慢采样硬件通路）

**目标**：确认 PB2(VBUS) 和 PB11(NTC) 的 ADC 原始值正确。

### 2.1 在 `program_task()` 中添加

```c
void program_task(void)
{
    static uint32_t last_ms = 0;
    uint32_t now_ms = g_tim6_tick_ms;
    if (now_ms == last_ms) return;
    last_ms = now_ms;

    /* ── 读 ADC2 DMA 缓冲 ── */
    uint16_t vbus_raw = g_adc2_dma_buf[0];
    uint16_t ntc_raw  = g_adc2_dma_buf[1];

    /* 码值 → 母线电压 */
    float vbus = (float)vbus_raw * ADC_REF_V / ADC_FULL_SCALE
               * (VBUS_R_UP + VBUS_R_DOWN) / VBUS_R_DOWN;

    /* 断点或 printf 观察 */
    (void)vbus;
    (void)ntc_raw;
}
```

> **验证点**：`vbus` ≈ 供电电压（如 24V 供电，应显示约 24V）；`ntc_raw` 随温度变化。

---

## 第3步：TIM1 PWM 手动输出测试（验证功率级硬件通路）

**目标**：手动控制六路 PWM 输出，示波器验证波形正确。

### 3.1 `program.h` 添加 debug PWM 结构体

```c
/* 在 program.h 中 #include 之后添加 */
typedef struct {
    uint8_t enable;
    float   duty_a;
    float   duty_b;
    float   duty_c;
} program_debug_pwm_t;

extern volatile program_debug_pwm_t g_debug_pwm;
```

### 3.2 `program.c` 添加 PWM 控制函数

```c
/* ── 全局 debug PWM 对象 ── */
volatile program_debug_pwm_t g_debug_pwm;

/* ── 启动六路互补 PWM ── */
static void program_start_tim1_pwm_outputs(void)
{
    /* 先写 50% 占空比 */
    __HAL_TIM_SET_COMPARE(&htim1, TIM_CHANNEL_1, 2125);
    __HAL_TIM_SET_COMPARE(&htim1, TIM_CHANNEL_2, 2125);
    __HAL_TIM_SET_COMPARE(&htim1, TIM_CHANNEL_3, 2125);

    HAL_TIM_PWM_Start(&htim1, TIM_CHANNEL_1);
    HAL_TIM_PWM_Start(&htim1, TIM_CHANNEL_2);
    HAL_TIM_PWM_Start(&htim1, TIM_CHANNEL_3);
    HAL_TIMEx_PWMN_Start(&htim1, TIM_CHANNEL_1);
    HAL_TIMEx_PWMN_Start(&htim1, TIM_CHANNEL_2);
    HAL_TIMEx_PWMN_Start(&htim1, TIM_CHANNEL_3);
}

/* ── 下发占空比到 TIM1 CCR ── */
static void program_apply_svpwm_to_tim1(float da, float db, float dc)
{
    uint32_t period = __HAL_TIM_GET_AUTORELOAD(&htim1) + 1;
    da = program_clamp_f32(da, 0.0f, 1.0f);
    db = program_clamp_f32(db, 0.0f, 1.0f);
    dc = program_clamp_f32(dc, 0.0f, 1.0f);

    __HAL_TIM_SET_COMPARE(&htim1, TIM_CHANNEL_1, (uint32_t)(da * period));
    __HAL_TIM_SET_COMPARE(&htim1, TIM_CHANNEL_2, (uint32_t)(db * period));
    __HAL_TIM_SET_COMPARE(&htim1, TIM_CHANNEL_3, (uint32_t)(dc * period));
}
```

### 3.3 在 `program_init()` 末尾添加

```c
    program_start_tim1_pwm_outputs();

    /* 默认关闭 debug PWM */
    g_debug_pwm.enable = 0;
    g_debug_pwm.duty_a = 0.30f;
    g_debug_pwm.duty_b = 0.40f;
    g_debug_pwm.duty_c = 0.60f;
```

//////////////////////////////  TODO 测试   //////////////////////////////
### 3.4 在 `program_task()` 开头添加

```c
    /* ── debug PWM 模式 ── */
    if (g_debug_pwm.enable) {
        program_apply_svpwm_to_tim1(g_debug_pwm.duty_a,
                           g_debug_pwm.duty_b,
                           g_debug_pwm.duty_c);
        HAL_GPIO_WritePin(N_SLEEP_GPIO_Port, N_SLEEP_Pin, GPIO_PIN_SET);
        return;  /* PWM 测试模式下跳过其他逻辑 */
    }
```

> **验证点**：调试器设 `g_debug_pwm.enable = 1`，示波器看 PA8/PA9/PA10 有对应占空比的互补 PWM 波形。死区时间约 500ns。

---

## 第4步：SPI1 读取 MA600A 编码器角度

**目标**：通过 SPI1 读取磁编码器绝对角度，验证角度通路。

### 4.1 创建 `App/ma600a.h`

```c
#ifndef MA600A_H
#define MA600A_H

#include "main.h"
#include "spi.h"
#include <stdint.h>

typedef struct {
    SPI_HandleTypeDef *hspi;
    GPIO_TypeDef      *cs_port;
    uint16_t           cs_pin;

    uint16_t angle_raw;         /* 0~65535 (16bit 分辨率) */
    float    angle_rad;         /* 0~2π 机械角 */
    float    angle_deg;         /* 0~360° */
    uint8_t  data_valid;

    uint32_t sample_counter;
    uint32_t reject_count;
    uint32_t comm_error_count;
    uint32_t consecutive_bad_count;
} ma600a_t;

void ma600a_init(ma600a_t *enc, SPI_HandleTypeDef *hspi,
                 GPIO_TypeDef *cs_port, uint16_t cs_pin);
uint8_t ma600a_read_angle(ma600a_t *enc);

#endif
```

### 4.2 创建 `App/ma600a.c`

```c
#include "ma600a.h"

/*
 * MA600A SPI 协议：16bit 帧，发送全 0 即读角度，返回格式：
 * ┌────────┬────────┬───────────────────────┐
 * │ bit15  │ bit14  │ bit13 ~ bit0          │
 * │ 保留   │ 有效位  │ 14bit 角度码 (0~16383) │
 * └────────┴────────┴───────────────────────┘
 * 精度：360° / 16384 ≈ 0.022°
 */

/* 读取角度的 SPI 命令字：发送 0x0000 即可获取当前角度 */
#define MA600A_CMD_ANGLE    0x0000

/* 提取 bit14 有效标志：0x4000 = 0100_0000_0000_0000 */
/* 若 (rx_data & 0x4000) == 0 → 数据无效，需丢弃 */
#define MA600A_ANGLE_VALID_MASK  0x4000

/* 提取 bit13~0 角度数据：0x3FFF = 0011_1111_1111_1111 */
/* 角度码 = rx_data & 0x3FFF，范围 0 ~ 16383 */
#define MA600A_ANGLE_DATA_MASK   0x3FFF

void ma600a_init(ma600a_t *enc, SPI_HandleTypeDef *hspi,
                 GPIO_TypeDef *cs_port, uint16_t cs_pin)
{
    enc->hspi    = hspi;
    enc->cs_port = cs_port;
    enc->cs_pin  = cs_pin;

    enc->angle_raw = 0;
    enc->angle_rad = 0.0f;
    enc->angle_deg = 0.0f;
    enc->data_valid = 0;

    enc->sample_counter       = 0;
    enc->reject_count         = 0;
    enc->comm_error_count     = 0;
    enc->consecutive_bad_count = 0;

    /* 片选初始拉高（不选中） */
    HAL_GPIO_WritePin(cs_port, cs_pin, GPIO_PIN_SET);
}

uint8_t ma600a_read_angle(ma600a_t *enc)
{
    uint16_t tx_data = MA600A_CMD_ANGLE;
    uint16_t rx_data = 0;

    /* 片选拉低，选中编码器 */
    HAL_GPIO_WritePin(enc->cs_port, enc->cs_pin, GPIO_PIN_RESET);

    /* 16bit 全双工传输 */
    HAL_StatusTypeDef status = HAL_SPI_TransmitReceive(enc->hspi,
        (uint8_t *)&tx_data, (uint8_t *)&rx_data, 1, 10);

    /* 片选拉高 */
    HAL_GPIO_WritePin(enc->cs_port, enc->cs_pin, GPIO_PIN_SET);

    enc->sample_counter++;

    if (status != HAL_OK) {
        enc->comm_error_count++;
        enc->consecutive_bad_count++;
        enc->data_valid = 0;
        return 0;
    }

    /* 检查数据有效标志 */
    if ((rx_data & MA600A_ANGLE_VALID_MASK) == 0) {
        enc->reject_count++;
        enc->consecutive_bad_count++;
        enc->data_valid = 0;
        return 0;
    }

    enc->consecutive_bad_count = 0;
    enc->angle_raw = rx_data & MA600A_ANGLE_DATA_MASK;

    /* 转换为角度 */
    enc->angle_deg = (float)enc->angle_raw * (360.0f / 16384.0f);
    enc->angle_rad = (float)enc->angle_raw * (6.28318530718f / 16384.0f);

    enc->data_valid = 1;
    return 1;
}
```

### 4.3 `program.c` 中加入编码器对象并初始化和读取

```c
/* 在 program.c 顶部添加 */
#include "ma600a.h"

static ma600a_t g_ma600a;

/* 在 program_init() 中添加（program_start_tim1_pwm_outputs() 之后） */
    ma600a_init(&g_ma600a, &hspi1, ENC_CS_GPIO_Port, ENC_CS_Pin);

/* 在 program_task() 中添加编码器读取 */
    /* ── 读编码器 ── */
    ma600a_read_angle(&g_ma600a);
    if (g_ma600a.data_valid) {
        /* 断点观察 angle_raw / angle_deg / angle_rad */
        uint16_t raw = g_ma600a.angle_raw;
        float deg    = g_ma600a.angle_deg;
        (void)raw; (void)deg;
    }
```

> **验证点**：用手转动电机轴，`angle_deg` 在 0°~360° 之间连续变化。

---

## 第5步：ADC1 电流零点校准

**目标**：功率级关闭时采集 ADC1 三通道均值作为零电流偏置。

### 5.1 `program.c` 添加电流偏置采集

```c
/* ── 电流采样硬件参数与校准常量 ── */
/* 零偏采集样本数：1024 个 × 100μs = 约 102ms 完成校准 */
#define CURRENT_OFFSET_TARGET_SAMPLES  1024
/* 采样电阻 10mΩ，串在电机相线上 */
#define CURRENT_SHUNT_OHM              0.01f
/* INA240 电流检测放大器固定增益 20V/V */
#define CURRENT_SENSE_GAIN             20.0f
/* 电流换算：I = (ADC码值 - 零偏码值) × 3.3V/4095 / (20 × 0.01Ω) */

/* ── 零偏采集状态 ── */
/* 零偏校准：在 ADC1 注入中断中累加，1024 次后取均值 */
static volatile uint32_t g_ia_offset_sum = 0;         /* A 相累加和 */
static volatile uint32_t g_ib_offset_sum = 0;         /* B 相累加和 */
static volatile uint32_t g_ic_offset_sum = 0;         /* C 相累加和 */
static volatile uint32_t g_offset_sample_count = 0;   /* 已采集样本数 */
static volatile uint8_t  g_offset_ready = 0;          /* 1=校准完成，中断中写，主循环读 */

/* 默认零偏 = 2048（12bit ADC 中点，对应 INA240 零电流输出 Vref/2 = 1.65V） */
static uint16_t g_ia_offset = 2048;
static uint16_t g_ib_offset = 2048;
static uint16_t g_ic_offset = 2048;

/* ── 启动 ADC1 注入组 ── */
static void program_start_adc1_injected(void)
{
    HAL_ADCEx_InjectedStart_IT(&hadc1);
}
```

### 5.2 `program_init()` 末尾添加

```c
    program_start_adc1_injected();
```

### 5.3 注入组完成中断回调

```c
/* ── ADC1 注入组转换完成 ── */
void HAL_ADCEx_InjectedConvCpltCallback(ADC_HandleTypeDef *hadc)
{
    if (hadc->Instance != ADC1) return;

    uint16_t ia_raw = (uint16_t)HAL_ADCEx_InjectedGetValue(hadc, ADC_INJECTED_RANK_1);
    uint16_t ib_raw = (uint16_t)HAL_ADCEx_InjectedGetValue(hadc, ADC_INJECTED_RANK_2);
    uint16_t ic_raw = (uint16_t)HAL_ADCEx_InjectedGetValue(hadc, ADC_INJECTED_RANK_3);

    /* 零偏采集阶段：累加 */
    if (!g_offset_ready) {
        g_ia_offset_sum += ia_raw;
        g_ib_offset_sum += ib_raw;
        g_ic_offset_sum += ic_raw;
        g_offset_sample_count++;

        if (g_offset_sample_count >= CURRENT_OFFSET_TARGET_SAMPLES) {
            g_ia_offset = (uint16_t)(g_ia_offset_sum / CURRENT_OFFSET_TARGET_SAMPLES);
            g_ib_offset = (uint16_t)(g_ib_offset_sum / CURRENT_OFFSET_TARGET_SAMPLES);
            g_ic_offset = (uint16_t)(g_ic_offset_sum / CURRENT_OFFSET_TARGET_SAMPLES);
            g_offset_ready = 1;
        }
    }
}
```

> **验证点**：1024 次采样后（约 100ms），`g_ia/g_ib/g_ic_offset` ≈ 2048（12bit ADC 中点），且 `g_offset_ready = 1`。

---

## 第6步：编码器零位对齐

**目标**：施加 Ud=1.8V 电压将转子锁定到电角度 0°，计算编码器电角偏置。

### 6.1 `program.c` 添加对齐状态

```c
/* Ud=1.8V 直流电压固定到 d 轴，将转子磁吸锁定到电角度 0° 位置 */
#define ALIGN_UD_V          1.80f
/* 对齐保持 8000 ticks × 100μs = 800ms，确保转子停止震荡后采样编码器偏置 */
#define ALIGN_HOLD_TICKS    8000

/* 编码器零位对齐：施加 Ud 电压将转子锁到 θe=0，记录此时的编码器角度作为偏置 */
static uint8_t  g_align_done = 0;          /* 1=对齐完成，后续所有电角度都减此偏置 */
static uint32_t g_align_counter = 0;       /* 对齐计时器，每 100μs 加 1，到 8000 完成 */
static float    g_elec_offset_rad = 0.0f;  /* 电角偏置：θe = 编码器电角 - offset */
```

### 6.2 在 `HAL_ADCEx_InjectedConvCpltCallback` 中追加对齐逻辑

在零偏就绪后（`g_offset_ready == 1` 且 `g_align_done == 0`），加入：

```c
    /* ── 零偏就绪后执行编码器零位对齐 ── */
    if (g_offset_ready && !g_align_done) {

        /* 调用 FOC 核心：Ud=1.8V 固定到 d 轴（θe=0），内部自动完成反Park + SVPWM */
        /* g_program_telemetry.vbus 由慢环 program_task() 1kHz 持续刷新，首次为 init 中默认 48V */
        foc_core_run_voltage_open_loop(&g_foc, ALIGN_UD_V, 0.0f, 0.0f, g_program_telemetry.vbus);
        program_apply_svpwm_to_tim1(&g_foc.duty);
        HAL_GPIO_WritePin(N_SLEEP_GPIO_Port, N_SLEEP_Pin, GPIO_PIN_SET);

        g_align_counter++;
        if (g_align_counter >= ALIGN_HOLD_TICKS) {
            /* 转子已被吸到 θe=0，记录此时编码器的电角度作为偏置 */
            float raw_elec = g_ma600a.angle_rad * MOTOR_POLE_PAIRS;  /* 极对数=14 */
            g_elec_offset_rad = motor_params_wrap_angle_rad(raw_elec);
            g_align_done = 1;
        }
    }
```

> **验证点**：等待 ~800ms 后 `g_align_done = 1`，`g_elec_offset_rad` 有有效值。电机转子应被锁定在一个固定位置。

---

## 第7步：开环拖动

**目标**：用积分器生成电角度，驱动电机以固定速度旋转。

### 7.1 添加开环状态

```c
#define OPEN_LOOP_SPEED_ELEC  2000.0f    /* 电角速度 rad/s */
#define OPEN_LOOP_UQ_V         1.00f

static float g_open_loop_theta = 0.0f;
```

### 7.2 在对齐完成后加入开环逻辑

```c
    /* ── 对齐完成后执行开环拖动 ── */
    if (g_align_done) {

        /* 电角度用积分器生成 */
        float dt = 0.0001f;   /* 100us ≈ 10kHz */
        g_open_loop_theta += OPEN_LOOP_SPEED_ELEC * dt;
        g_open_loop_theta = program_wrap_angle_0_2pi(g_open_loop_theta);

        float ud = 0.0f;
        float uq = OPEN_LOOP_UQ_V;
        float theta = g_open_loop_theta;

        /* 反Park */
        float sin_t = sinf(theta);
        float cos_t = cosf(theta);
        float u_alpha = ud * cos_t - uq * sin_t;
        float u_beta  = ud * sin_t + uq * cos_t;

        /* SVPWM（同上） */
        float vbus = program_clamp_f32(
            (float)g_adc2_dma_buf[0] * 3.3f / 4095.0f
            * (240000.0f + 10000.0f) / 10000.0f,
            1.0f, 100.0f);

        float va = u_alpha;
        float vb = -0.5f * u_alpha + 0.86602540378f * u_beta;
        float vc = -0.5f * u_alpha - 0.86602540378f * u_beta;

        float vmax = va; if (vb > vmax) vmax = vb; if (vc > vmax) vmax = vc;
        float vmin = va; if (vb < vmin) vmin = vb; if (vc < vmin) vmin = vc;
        float v_offset = (vmax + vmin) * 0.5f;
        va -= v_offset; vb -= v_offset; vc -= v_offset;

        float da = 0.5f + va / vbus;
        float db = 0.5f + vb / vbus;
        float dc = 0.5f + vc / vbus;

        program_apply_svpwm_to_tim1(da, db, dc);
    }
```

> **验证点**：电机开始旋转，转速大致为 `2000 / 14 = ~143 rad/s = ~1365 rpm`。用手捏住轴应有扭矩。

---

## 第8步：闭环 FOC 控制

### 8.1 速度测量（基于编码器差分）

```c
#define SPEED_OBSERVER_WINDOW  20U     /* 20个采样窗口 */

static float g_prev_mech_angle = 0.0f;
static float g_continuous_mech_rad = 0.0f;
static uint32_t g_speed_window_count = 0;
static float g_speed_window_start = 0.0f;
static float g_speed_meas_mech = 0.0f;      /* 机械角速度 rad/s */
static uint8_t g_speed_ready = 0;

/* 在注入中断中，对齐完成后加入速度观测 */
/**
 * 速度观测：基于编码器机械角差分
 * 首次调用记录基准角度，后续每拍累加角差（处理 0/360 跳变）
 * 每 SPEED_OBSERVER_WINDOW 拍（20拍×100μs=2ms）计算一次窗口内平均速度
 * @param mech_angle  编码器机械角 (rad)
 */
static void program_update_speed_measurement(float mech_angle)
{
    /** 首次调用：初始化基准角度和窗口起点 */
    if (!g_speed_ready) {
        g_prev_mech_angle    = mech_angle;
        g_continuous_mech_rad = mech_angle;
        g_speed_window_start = mech_angle;
        g_speed_window_count = 0;
        g_speed_ready = 1;
        return;
    }

    /** 计算本拍角差（wrap_delta 处理 0°/360° 跳变） */
    float delta = program_wrap_delta_pm_pi(mech_angle - g_prev_mech_angle);
    g_prev_mech_angle = mech_angle;
    g_continuous_mech_rad += delta;   /** 连续累加机械角 */

    /** 窗口累积 */
    g_speed_window_count++;
    if (g_speed_window_count >= SPEED_OBSERVER_WINDOW) {
        /** 满 20 拍：速度 = (终点角 - 起点角) / 时间 */
        float dt = 0.0001f * (float)g_speed_window_count;
        g_speed_meas_mech = (g_continuous_mech_rad - g_speed_window_start) / dt;
        g_speed_window_start = g_continuous_mech_rad;  /** 重置窗口起点 */
        g_speed_window_count = 0;                      /** 开启下一窗口 */
    }
}
```

### 8.2 电流换算

> ?? 以下模块为第8步闭环 FOC 必需的完整组件，按依赖顺序逐个创建文件。

#### 8.2.0 创建 `App/motor_params.h` — 电机参数与辅助函数

**参考**：`program/App/motor_params.h`

```c
#ifndef MOTOR_PARAMS_H
#define MOTOR_PARAMS_H

#include <math.h>

#define MOTOR_POLE_PAIRS         14.0f
#define MOTOR_GEAR_RATIO         8.0f
#define MOTOR_ENCODER_ON_OUTPUT_SHAFT 0U
#define MOTOR_ENCODER_DIRECTION_SIGN (-1.0f)
#define MOTOR_TWO_PI             6.28318530718f

static inline float motor_params_wrap_angle_rad(float angle_rad)
{
    if (!isfinite(angle_rad)) return 0.0f;
    float turns = floorf(angle_rad / MOTOR_TWO_PI);
    angle_rad -= turns * MOTOR_TWO_PI;
    if (angle_rad < 0.0f) angle_rad += MOTOR_TWO_PI;
    else if (angle_rad >= MOTOR_TWO_PI) angle_rad -= MOTOR_TWO_PI;
    return angle_rad;
}

static inline float motor_params_rotor_mech_to_elec_rad(float rotor_mech_angle_rad)
{ return motor_params_wrap_angle_rad(rotor_mech_angle_rad * MOTOR_POLE_PAIRS); }

static inline float motor_params_encoder_mech_to_rotor_mech_rad(float encoder_mech_angle_rad)
{
#if MOTOR_ENCODER_ON_OUTPUT_SHAFT
    return encoder_mech_angle_rad * MOTOR_ENCODER_DIRECTION_SIGN * MOTOR_GEAR_RATIO;
#else
    return encoder_mech_angle_rad * MOTOR_ENCODER_DIRECTION_SIGN;
#endif
}
#endif
```

#### 8.2.1 创建 `App/drv_pid.h` + `App/drv_pid.c` — Q15 定点 PI 控制器

> **关键差异**：参考项目用 Q15 定点格式而非浮点 PI。Q15 格式在 Cortex-M4 上执行更快（无需 FPU 介入），且带完整抗饱和处理。

**drv_pid.h**：

```c
#ifndef _DRV_PID_
#define _DRV_PID_

#include <stdint.h>

typedef struct {
    int32_t ref, feedback, error;
    int32_t kp_q15, ki_q15;
    int32_t p_out, i_out, output;
    int32_t i_term_q15;
    int32_t out_min, out_max;
} drv_pid_pi_t;

void drv_pid_pi_init(drv_pid_pi_t *pid, int32_t kp_q15, int32_t ki_q15,
                     int32_t out_min, int32_t out_max, int32_t out_init);
void drv_pid_pi_reset(drv_pid_pi_t *pid, int32_t out_init);
int32_t drv_pid_pi_step(drv_pid_pi_t *pid, int32_t ref, int32_t feedback);

#endif
```

**drv_pid.c**（完整实现见上文 ③ drv_pid.h / drv_pid.c，共 157 行）

> **Q15 格式说明**：`kp_q15 = kp × 2^15`。如 `kp = 0.5` → `kp_q15 = 16384`。积分项内部在 Q15 域计算，输出四舍五入还原到 int32。

**测试**：
| # | 测试项 | 方法 | 通过标准 |
|---|--------|------|---------|
| PID.1 | 阶跃响应 | `ref=1000, fb=0, kp=16384, ki=0` | `output` 立即 = 500（Q15: 0.5×1000） |
| PID.2 | 积分作用 | `ref=1000, fb=0, kp=0, ki=163` | 每拍递增 |
| PID.3 | 抗饱和 | `ref=1000, fb=0, out_max=200` | `output` ≤ 200 |

#### 8.2.2 创建 `App/filter.h` + `App/filter.c` — 一阶低通滤波器

**参考**：`program/App/filter.h` + `filter.c`

```c
// filter.h
typedef struct { float alpha; float value; uint8_t initialized; } filter_lpf_f32_t;
typedef struct { int32_t alpha_q15; int32_t value_q15; uint8_t initialized; } filter_lpf_s32_t;

void filter_lpf_f32_init(filter_lpf_f32_t *f, float alpha, float init);
float filter_lpf_f32_update(filter_lpf_f32_t *f, float input);
void filter_lpf_s32_init(filter_lpf_s32_t *f, int32_t alpha_q15, int32_t init);
int32_t filter_lpf_s32_update(filter_lpf_s32_t *f, int32_t input);
```

> **公式**：`y += α(x ? y)`。α 越小滤波越强。用于速度测量的 LPF 后处理、VBUS 电压平滑。

#### 8.2.3 电流换算

```c
/* ADC 原始值 → 相电流(A) */
static float convert_current(uint16_t raw, uint16_t offset)
{
    float voltage = ((float)raw - (float)offset) * 3.3f / 4095.0f;
    return voltage / (CURRENT_SENSE_GAIN * CURRENT_SHUNT_OHM);
}
```

### 8.3 PI 控制器 + 闭环 FOC 主逻辑

**文件**：`program_current.c` 的 `program_current_fast_loop()`，在对齐完成后替换开环拖动。

```c
// ── 电流/速度环 PI 参数 ──
/** 电流环 PI 参数：比例增益（带宽 1kHz） */
#define CURRENT_KP       5.15f
/** 电流环 PI 参数：积分增益 */
#define CURRENT_KI    9110.6f
/** 速度环 PI 参数：比例增益 */
#define SPEED_KP        0.0015f
/** 速度环 PI 参数：积分增益 */
#define SPEED_KI        0.015f
/** q 轴电流限幅 (A) */
#define IQ_LIMIT_A      12.0f

/** d 轴电流 PI 积分项 */
static float g_id_integral = 0.0f;
/** q 轴电流 PI 积分项 */
static float g_iq_integral = 0.0f;
/** 速度环 PI 积分项 */
static float g_speed_integral = 0.0f;

/**
 * 带抗饱和的浮点 PI 控制器
 * @param ref       目标值
 * @param fb        反馈值
 * @param kp        比例增益
 * @param ki        积分增益
 * @param dt        控制周期 (s)
 * @param integral  积分项指针（跨调用保持）
 * @param lo        输出下限
 * @param hi        输出上限
 * @return 限幅后的 PI 输出
 */
static float run_pi_f32(float ref, float fb, float kp, float ki,
                         float dt, float *integral, float lo, float hi)
{
    /** 计算误差 */
    float error = ref - fb;
    /** 比例项 */
    float p_out = kp * error;
    /** 积分候选值 = 上次积分 + ki × dt × error */
    float i_cand = *integral + ki * dt * error;
    /** 积分项限幅 */
    i_cand = program_clamp_f32(i_cand, lo, hi);
    /** 总输出 = P + I */
    float out = p_out + i_cand;
    /** 抗饱和：仅在不加深饱和的方向更新积分 */
    if (out > hi)      { out = hi; if (error < 0.0f) *integral = i_cand; }
    else if (out < lo) { out = lo; if (error > 0.0f) *integral = i_cand; }
    else              { *integral = i_cand; }
    return out;
}

// ── 闭环 FOC 主逻辑（替换 program_open_loop_svpwm 调用） ──
if (g_align_done) {
    // ① 电流换算
    float ia = convert_current(ia_raw, g_ia_offset);
    float ib = convert_current(ib_raw, g_ib_offset);

    // ② Clarke：ia/ib → αβ（ic = -ia-ib 自动满足）
    float i_alpha = ia;
    float i_beta  = (ia + 2.0f * ib) / 1.73205080757f;

    // ③ 获取电角度
    float mech_angle = g_ma600a.angle_rad;
    float elec_angle = program_wrap_angle_0_2pi(mech_angle * 14.0f - g_elec_offset_rad);

    // ④ Park：αβ → dq
    float sin_t = sinf(elec_angle);
    float cos_t = cosf(elec_angle);
    float id =  i_alpha * cos_t + i_beta * sin_t;
    float iq = -i_alpha * sin_t + i_beta * cos_t;

    // ⑤ 速度环 PI（200Hz 级联）→ iq_ref
    float iq_ref = 0.0f;
    if (g_speed_ready && (g_speed_window_count == 0)) {
        float speed_dt = 0.0001f * SPEED_OBSERVER_WINDOW;
        iq_ref = run_pi_f32(g_speed_ref, g_speed_meas_mech,
                            SPEED_KP, SPEED_KI, speed_dt,
                            &g_speed_integral, -IQ_LIMIT_A, IQ_LIMIT_A);
    }

    // ⑥ 母线电压 & 限幅
    float vbus = program_clamp_f32(
        (float)g_adc2_dma_buf[0] * 3.3f / 4095.0f * 25.0f, 1.0f, 100.0f);
    float v_limit = vbus * 0.577f;   // vbus/√3

    // ⑦ 电流环 PI：id→0, iq→iq_ref
    float ud = run_pi_f32(0.0f, id, CURRENT_KP, CURRENT_KI, 0.0001f,
                          &g_id_integral, -v_limit, v_limit);
    float uq = run_pi_f32(iq_ref, iq, CURRENT_KP, CURRENT_KI, 0.0001f,
                          &g_iq_integral, -v_limit, v_limit);

    // ⑧ 电压矢量限幅
    float v_mag = sqrtf(ud * ud + uq * uq);
    if (v_mag > v_limit && v_mag > 0.0f) {
        float scale = v_limit / v_mag;
        ud *= scale; uq *= scale;
    }

    // ⑨ 反Park + SVPWM（复用 foc_core）
    foc_core_run_voltage_open_loop(&g_foc, ud, uq, elec_angle, vbus);
    program_apply_svpwm_to_tim1(&g_foc.duty);
    HAL_GPIO_WritePin(N_SLEEP_GPIO_Port, N_SLEEP_Pin, GPIO_PIN_SET);
}
```

> **验证点**：调试器设 `g_speed_ref = 100`（rad/s），电机闭环稳速运行。`id ≈ 0`，`iq` 随负载变化。

### 8.4 测试

| # | 测试项 | 方法 | 通过标准 |
|---|--------|------|---------|
| 8.1 | 速度测量 | 手转电机轴，看 `g_speed_meas_mech` | 值随转速变化 |
| 8.2 | id 趋零 | 空载运行时看 `id` | `id ≈ 0`（±0.5A 内） |
| 8.3 | 速度跟踪 | 改 `g_speed_ref` 50→100→200 | 实际转速跟随 |
| 8.4 | 负载扰动 | 手捏电机轴后松开 | 转速跌落后恢复 |
| 8.5 | 正反转 | `g_speed_ref` 设负值 | 平滑切换 |
| 8.6 | 稳态电流 | 空载观察 `iq` | < 2A |
| 8.7 | 长时间运行 | 运行 5 分钟 | 无过热异响 |

---

## 完整开发路线总结

```
第1步: 最小框架          ████                     编译通过
第2步: ADC2 读数验证      ████████                 vbus 正确
第3步: TIM1 PWM 测试      ████████████             波形 OK
第4步: SPI1 编码器        ████████████████          角度 0~360°
第5步: ADC1 零偏校准      ████████████████████      offset≈2048
第6步: 编码器零位对齐      ████████████████████████  转子锁定
第7步: 开环拖动            ████████████████████████████  电机旋转
8.2.0: motor_params.h     █████████████████████████████  参数定义
8.2.1: drv_pid.c/h         ██████████████████████████████  Q15 PID
8.2.2: filter.c/h          ███████████████████████████████  LPF 滤波器
第8步: 闭环 FOC            █████████████████████████████████  三环控制
第9步: 状态机驱动          ██████████████████████████████████  自动管理
第10步: 异常保护           ████████████████████████████████████  100% 复原
```

### Keil 工程文件清单（完整编译所需）

| 分组 | 文件 |
|------|------|
| **App/** | `program.c`, `program.h`, `foc_core.c`, `foc_core.h` |
| | `drv_pid.c`, `drv_pid.h` ← Q15 PI 控制器 |
| | `filter.c`, `filter.h` ← 一阶 LPF |
| | `motor_params.h` ← 电机参数+内联函数 |
| | `motor_state.c`, `motor_state.h` ← 六状态机 |
| | `ma600a.c`, `ma600a.h` ← 编码器驱动 |
| **Core/Src/** | `main.c`, `adc.c`, `tim.c`, `spi.c`, `dma.c`, `gpio.c` |
| **Include Paths** | `App/`, `Core/Inc/`, `Drivers/...` |

### 文件创建顺序（按依赖关系）

```
① motor_params.h      ← 无依赖，最先创建
② filter.h + filter.c   ← 无依赖
③ drv_pid.h + drv_pid.c ← 无依赖
④ foc_core.h + foc_core.c ← 依赖 motor_params.h
⑤ motor_state.h + motor_state.c ← 依赖 ②③④
⑥ ma600a.h + ma600a.c   ← 依赖 SPI
⑦ program.h              ← 依赖 ④⑤⑥
⑧ program.c              ← 依赖 ①~⑦，最终集成
```

### main.c 对接

在 CubeMX 生成的 `main.c` 中：

```c
/* USER CODE BEGIN Includes */
#include "program.h"
/* USER CODE END Includes */

/* main() 中，所有 MX_xxx_Init() 之后 */
/* USER CODE BEGIN 2 */
program_init();
/* USER CODE END 2 */

/* while(1) 中 */
/* USER CODE BEGIN 3 */
program_task();
/* USER CODE END 3 */
```

**HAL 回调转发**（放在 stm32g4xx_it.c 或单独文件）：

```c
void HAL_TIM_PeriodElapsedCallback(TIM_HandleTypeDef *htim)
{ program_tim_period_elapsed_callback(htim); }

void HAL_ADCEx_InjectedConvCpltCallback(ADC_HandleTypeDef *hadc)
{ program_adc_injected_conv_cplt_callback(hadc); }

void HAL_SPI_TxRxCpltCallback(SPI_HandleTypeDef *hspi)
{ ma600a_spi_txrx_cplt_callback(&g_ma600a, hspi); }

void HAL_SPI_ErrorCallback(SPI_HandleTypeDef *hspi)
{ ma600a_spi_error_callback(&g_ma600a, hspi); }
```

### 首次上电流程

```
上电 → program_init()
  ├── ADC 校准
  ├── foc_core_init() + motor_state_init()
  ├── ma600a_init() + 读取首帧角度
  ├── program_start_tim1_pwm_outputs()  → 50% 占空比
  ├── program_start_adc2_dma_chain()    → TIM6 + DMA 启动
  └── HAL_ADCEx_InjectedStart_IT()      → 注入组开始采样

快环（10kHz ISR）：
  零偏校准(1024次) → 编码器读取 → 速度观测
  → 对齐(Ud锁转子800ms) → 开环拖动(1.5s)
  → 闭环FOC(id=0, 速度PI→iq_ref→电流PI→SVPWM)

慢环（1kHz program_task）：
  读ADC2(vbus/ntc) → 编码器遥测 → 状态显示

启动命令：g_motor.run_request = 1
停机命令：g_motor.run_request = 0
```

> **核心原则**：每一步都独立验证硬件通路，不跳步，不贪多。前一步不通，绝不进下一步。

---

## 各阶段功能点与测试方法

> 每完成一步，必须通过全部测试项才能进入下一步。
> 测试工具：Keil 调试器（断点/变量观察）+ 示波器 + 万用表。

---

### 第1步测试：最小框架搭建

| 编号 | 功能点 | 测试方法 | 通过标准 |
|------|--------|---------|---------|
| 1.1 | 编译通过 | Keil 点击 Build (F7) | 0 Error, 0 Warning |
| 1.2 | 系统时钟正常 | 调试器中查看 `SystemCoreClock` 变量 | = 170000000 (170MHz) |
| 1.3 | `program_init()` 被调用 | 在 `program_init()` 第一行打断点，复位运行 | 断点命中 |
| 1.4 | `program_task()` 循环运行 | 在 `program_task()` 中 `last_ms = now_ms` 后打断点 | 每秒命中约 1000 次 |
| 1.5 | TIM6 时基正常 | 调试器中观察 `g_tim6_tick_ms` | 每秒递增约 1000 |
| 1.6 | 功率级默认休眠 | 万用表测 PB14 引脚电压 | 0V（低电平，休眠状态） |

**常见问题排查**：
- 编译不过 → 检查 Keil 工程是否添加了 `App/program.c`，Include Paths 是否包含 `App/`
- `g_tim6_tick_ms` 不变化 → 检查 CubeMX 中 TIM6 是否勾选 Activated、NVIC 中断是否使能
- 程序跑飞 → 检查 `main.c` 中 `program_init()` 和 `program_task()` 是否放在正确的 USER CODE 区域

---

### 第2步测试：ADC2 读数验证

| 编号 | 功能点 | 测试方法 | 通过标准 |
|------|--------|---------|---------|
| 2.1 | DMA 缓冲区有数据 | 调试器中观察 `g_adc2_dma_buf[0]` 和 `g_adc2_dma_buf[1]` | 两个值都在 0~4095 范围内，且不断变化 |
| 2.2 | VBUS 原始值合理 | `g_adc2_dma_buf[0]` 的值 | 供电 24V 时约为 `24 / 25 / 3.3 × 4095 ≈ 1190` |
| 2.3 | VBUS 电压换算正确 | 计算 `vbus = raw × 3.3 / 4095 × 25` | 与万用表实测母线电压误差 < 5% |
| 2.4 | NTC 原始值合理 | `g_adc2_dma_buf[1]` 的值 | 常温下约 1500~2500（取决于分压电路） |
| 2.5 | 数据实时更新 | 在 `program_task()` 中连续观察两轮 | 每 1ms 更新一次，值非固定 |

**硬件检查清单**：
- 万用表测 PB2 对地电压 → 应为 VBUS/25
- 万用表测 PB11 对地电压 → 应在 0~3.3V 之间
- 确认 ADC2 外部触发源是 TIM6_TRGO（在 CubeMX 中检查）

---

### 第3步测试：TIM1 PWM 手动输出

| 编号 | 功能点 | 测试方法 | 通过标准 |
|------|--------|---------|---------|
| 3.1 | TIM1 计数器在跑 | 调试器中观察 `htim1.Instance->CNT` | 值在 0~4249 之间周期性变化 |
| 3.2 | 六路 PWM 全部有波形 | 示波器依次测 PA8/PA9/PA10/PC13/PB0/PB1 | 每路都有 20kHz 方波 |
| 3.3 | PWM 频率正确 | 示波器测 PA8 频率 | ≈ 20kHz (170MHz / 4250 / 2) |
| 3.4 | 占空比可控 | 修改 `g_debug_pwm.duty_a` 为 0.3/0.5/0.7，示波器观察 | 占空比相应变化（30%/50%/70%） |
| 3.5 | 互补输出正确 | 示波器同时测 PA8(CH1) 和 PC13(CH1N) | 波形互补，死区约 500ns |
| 3.6 | 死区时间正确 | 示波器放大观察 CH1 和 CH1N 的跳变沿 | 死区间隔 ≈ 500ns (85/170MHz) |
| 3.7 | 功率级可控 | 调试器设 `g_debug_pwm.enable = 1`，万用表测 PB14 | PB14 变为高电平（~3.3V） |
| 3.8 | 50% 占空比默认安全 | 调试器设 `g_debug_pwm.enable = 0`，PWM 启动后默认波形 | 三路均为 50% 占空比 |

**?? 安全警告**：
- PWM 测试阶段 **不要接电机**，先空载确认波形正确
- 务必确认死区存在，否则上下桥直通会烧 MOSFET
- 先用低压电源（如 12V）测试，确认无误后再上额定电压

---

### 第4步测试：SPI1 编码器读取

| 编号 | 功能点 | 测试方法 | 通过标准 |
|------|--------|---------|---------|
| 4.1 | SPI 通信正常 | 在 `ma600a_read_angle()` 返回后打断点，看 `status` | `status == HAL_OK` |
| 4.2 | 角度数据有效 | 调试器观察 `g_ma600a.data_valid` | = 1 |
| 4.3 | 角度范围正确 | 用手慢转电机，观察 `g_ma600a.angle_deg` | 0°~360° 连续变化，无跳变 |
| 4.4 | 角度连续性 | 用手转一圈，观察 `angle_deg` | 能从 0 连续变到 360，再从 0 开始 |
| 4.5 | 通信无异常 | 调试器观察 `g_ma600a.comm_error_count` 和 `reject_count` | 两者均为 0（或极小值） |
| 4.6 | 片选时序正常 | 逻辑分析仪或示波器观察 PA4(ENC_CS) | 每次读数前拉低、读完后拉高 |

**硬件检查清单**：
- 万用表测 PA4 → 高电平（~3.3V，未选中）
- 示波器测 PA5(SCK) → 有 16 个时钟脉冲（每次读角度时）
- 检查 SPI1 配置：Mode 3 (CPOL=High, CPHA=2Edge), 16bit
- 编码器供电是否正常（MA600A 通常 3.3V 或 5V）

---

### 第5步测试：ADC1 电流零点校准

| 编号 | 功能点 | 测试方法 | 通过标准 |
|------|--------|---------|---------|
| 5.1 | ADC1 注入中断触发 | 在 `HAL_ADCEx_InjectedConvCpltCallback` 中打断点 | 断点以 ~10kHz 频率命中 |
| 5.2 | 三相原始值可读 | 调试器观察 `ia_raw/ib_raw/ic_raw` | 三个值在 2000~2100 范围内 |
| 5.3 | 零偏校准完成 | 等待约 100ms 后观察 `g_offset_ready` | = 1 |
| 5.4 | 零偏值合理 | 调试器观察 `g_ia_offset / g_ib_offset / g_ic_offset` | 三个值都 ≈ 2048（±100） |
| 5.5 | 三相一致性 | 比较三个 offset 值 | 三者相差 < 50 |
| 5.6 | 采样计数正确 | 观察 `g_offset_sample_count` 最终值 | = CURRENT_OFFSET_TARGET_SAMPLES (1024) |

**注意事项**：
- 校准时功率级必须关闭（电机不通电），否则电流不为零
- 如果 offset 偏离 2048 超过 ±200，检查硬件运放偏置是否正常
- INA240 系列电流采样放大器输出偏置默认在 Vref/2 ≈ 1.65V（对应 ADC 码值 2048）

---

### 第6步测试：编码器零位对齐

| 编号 | 功能点 | 测试方法 | 通过标准 |
|------|--------|---------|---------|
| 6.1 | 对齐过程启动 | 设 `g_motor.run_request = 1`，观察状态 | `g_motor.state == MOTOR_STATE_ALIGN` |
| 6.2 | 功率级使能 | 万用表测 PB14 | 变为高电平（~3.3V） |
| 6.3 | Ud 电压输出 | 示波器观察 PA8/PA9/PA10 占空比 | 三路占空比不再全是 50%，有差异 |
| 6.4 | 转子被锁定 | 用手尝试转动电机轴 | 有明显阻力，转子被电磁力锁定 |
| 6.5 | 对齐完成 | 等待约 800ms 后观察 `g_align_done` | = 1 |
| 6.6 | 偏置角有效 | 观察 `g_elec_offset_rad` | 为 0~2π 之间的浮点值 |
| 6.7 | 对齐计数器 | 观察 `g_align_counter` 最终值 | = ALIGN_HOLD_TICKS (8000) |
| 6.8 | PCM 模式恢复 | `run_request = 0` 后观察 PB14 | PB14 变低，PWM 回到 50% |

**?? 注意**：
- 对齐时电机会有轻微抖动或发出高频声音，这是正常现象
- 如果电机剧烈抖动或过流，立即断电，检查 Ud 电压是否过大
- 对齐过程中电机轴会有微小转动（寻找 d 轴方向），确保负载可以自由转动

---

### 第7步测试：开环拖动

| 编号 | 功能点 | 测试方法 | 通过标准 |
|------|--------|---------|---------|
| 7.1 | 电机开始旋转 | 设 `run_request = 1`，等待对齐完成后观察 | 电机轴持续旋转 |
| 7.2 | 旋转方向一致 | 目测旋转方向 | 始终同一方向，不来回摆动 |
| 7.3 | 转速大致正确 | 用手掐表计时 10 圈，计算 RPM | 与 `OPEN_LOOP_SPEED_ELEC / 14 × 60 / 2π` 接近 |
| 7.4 | 有输出扭矩 | 用手轻捏电机轴 | 有持续扭矩，不会轻易停转 |
| 7.5 | VBUS 电压正常 | 调试器观察 `g_program_telemetry.vbus` | 与电源电压一致，无明显跌落 |
| 7.6 | 编码器角度连续 | 观察 `g_ma600a.angle_deg` | 连续变化，无跳变 |
| 7.7 | 停机正常 | 设 `run_request = 0` | 电机停转，PWM 回到 50%，PB14 变低 |

**调试技巧**：
- 如果电机不转但有声音 → 增大 `OPEN_LOOP_UQ_V`（如从 1.0V 提到 2.0V）
- 如果电机抖动不转 → 检查三相线序是否正确，尝试交换任意两相
- 如果转速不对 → 检查 `OPEN_LOOP_SPEED_ELEC` 和极对数配置

---

### 第8步测试：闭环 FOC 控制

| 编号 | 功能点 | 测试方法 | 通过标准 |
|------|--------|---------|---------|
| 8.1 | 速度测量就绪 | 观察 `g_enc_speed_ok` | = 1（启动后约 20 个采样周期） |
| 8.2 | 速度跟踪能力 | 修改 `g_speed_ref` (如 50→100 rad/s)，观察实际转速 | 实际转速跟随目标，稳态误差 < 5% |
| 8.3 | 电流环工作 | 观察 `g_program_telemetry.id` 和 `iq` | id ≈ 0, iq 随负载变化 |
| 8.4 | 负载扰动恢复 | 用手捏电机轴增加负载后松开 | 转速短暂下降后恢复目标值 |
| 8.5 | 正反转切换 | `g_speed_ref` 设正值/负值 | 电机正转/反转，切换平滑 |
| 8.6 | 启动无过流 | 启动瞬间观察 `iq` | iq 不超过 `g_iq_limit` (12A) |
| 8.7 | 稳态电流小 | 空载稳定运行时观察 `iq` | iq < 2A（视电机和摩擦而定） |
| 8.8 | 快环不超时 | 观察 `g_program_telemetry.fast_loop_overrun` | = 0（始终在 100μs 内完成） |
| 8.9 | 故障保护有效 | 短接 PB15 到 GND（模拟 nFAULT 拉低） | 电机立即停转，PB14 变低 |
| 8.10 | 长时间运行稳定 | 让电机运行 5 分钟以上 | 无过热、无异响、无异常停机 |

**调试技巧**：
- 速度环振荡 → 降低 `g_speed_kp` / `g_speed_ki`
- 电流环振荡 → 降低 `g_current_kp` / `g_current_ki`
- 电机无力 → 增大 `g_iq_limit`，检查 vbus 测量是否正确
- 快环超时 → 检查是否有浮点运算过多，可考虑启用 FPU

---

## 测试工具速查

| 工具 | 用途 | 关键操作 |
|------|------|---------|
| **Keil 调试器** | 断点、变量观察 | 在关键函数打断点，Watch 窗口添加全局变量 |
| **示波器** | PWM 波形、死区、SPI 时序 | 测 PA8/PA9/PA10，检查频率和占空比 |
| **万用表** | 静态电压、引脚电平 | 测 PB2(VBUS分压)、PB14(使能)、PB15(故障) |
| **逻辑分析仪** | SPI 通信时序 | 接 PA4(CS)/PA5(SCK)/PA6(MISO)/PA7(MOSI) |

## 常见故障速查表

| 现象 | 可能原因 | 排查方向 |
|------|---------|---------|
| 编译报错 "undefined reference" | 缺少源文件或头文件路径 | 检查 Keil 工程设置 |
| 程序卡在 `Error_Handler()` | HAL 初始化失败 | 检查外设时钟是否使能 |
| PWM 无输出 | TIM1 未启动 | 确认调用了 `program_start_tim1_pwm_outputs()` |
| ADC 值始终为 0 | 触发源配置错误 | 检查 TIM1_TRGO2 / TIM6_TRGO 配置 |
| 编码器读数不变 | SPI 通信失败 | 检查 Mode3 / 16bit / 片选电平 |
| 电机不转 | 功率级未使能 | 万用表测 PB14 电平 |
| 电机抖动不转 | 线序错误 | 交换任意两相电机线 |
| 上电即过流 | 死区不足 | 示波器确认死区 > 300ns |
| 快环超时 | 中断中耗时过长 | 检查是否有 printf 或阻塞调用在中断中 |

---

## 补充：位置环与高级控制功能

> 以下功能是完整版 program.c 中必须的部分，在仿写步骤中未展开，在此集中补充。

### 补充1：电压矢量限幅

**文件位置**：`program.c` 的 `program_limit_voltage_vector()`

**功能**：限制 dq 电压矢量长度不超过 SVPWM 调制范围（vbus × 0.577），防止过调制。

```c
/* 限制 dq 电压矢量半径不超过 v_limit */
static void program_limit_voltage_vector(float *ud_ref, float *uq_ref, float v_limit)
{
    if ((ud_ref == 0) || (uq_ref == 0)) return;
    if ((!isfinite(*ud_ref)) || (!isfinite(*uq_ref)) || (!isfinite(v_limit)) || (v_limit <= 0.0f)) {
        *ud_ref = 0.0f;
        *uq_ref = 0.0f;
        return;
    }
    float v_mag = sqrtf((*ud_ref * *ud_ref) + (*uq_ref * *uq_ref));
    if ((v_mag > v_limit) && (v_mag > 0.0f)) {
        float v_scale = v_limit / v_mag;
        *ud_ref *= v_scale;
        *uq_ref *= v_scale;
    }
}

/* 获取电压限幅值：vbus × 调制比上限 */
static float program_get_voltage_limit_v(void)
{
    float vbus = g_program_telemetry.vbus;
    return 0.57735026919f * ((vbus > 1.0f) ? vbus : 1.0f);
}
```

### 补充2：电流/速度参考斜坡

**功能**：防止电流或速度指令突变，平滑过渡避免转矩冲击。

```c
#define CURRENT_REF_RAMP_A_PER_S   150.0f    /* 电流斜坡 150A/s */
#define SPEED_REF_RAMP_RAD_S2      100.0f    /* 速度斜坡 100 rad/s? */

static float g_id_ref_applied_a = 0.0f;   /* 实际生效的 id 给定 */
static float g_iq_ref_applied_a = 0.0f;   /* 实际生效的 iq 给定 */

/**
 * @brief  电流给定斜坡函数 — 对 id/i_q 指令进行斜率限制，避免电流突变造成转矩冲击
 *
 * 每次快环(FOC)调用时，将目标电流 (g_motor.id_ref / iq_ref) 与当前已生效的给定值
 * 做差分，并以 CURRENT_REF_RAMP_A_PER_S 为上限的单步变化量逐步逼近目标值，
 * 从而实现类似"低通滤波"的软启动效果。同时用 iq_limit 对目标值进行钳位保护。
 *
 * 调用时机：每个 FOC 快环周期 (FAST_LOOP_DT_S) 调用一次
 */
static void program_update_applied_current_references(void)
{
    float current_step_a = CURRENT_REF_RAMP_A_PER_S * FAST_LOOP_DT_S;
    float id_target = program_clamp_f32(g_motor.id_ref, -g_motor.iq_limit, g_motor.iq_limit);
    float iq_target = program_clamp_f32(g_motor.iq_ref, -g_motor.iq_limit, g_motor.iq_limit);

    /* 单步斜率限制 */
    float delta_id = id_target - g_id_ref_applied_a;
    delta_id = program_clamp_f32(delta_id, -current_step_a, current_step_a);
    g_id_ref_applied_a += delta_id;

    float delta_iq = iq_target - g_iq_ref_applied_a;
    delta_iq = program_clamp_f32(delta_iq, -current_step_a, current_step_a);
    g_iq_ref_applied_a += delta_iq;
}

/**
 * @brief  速度给定斜坡函数 — 对机械速度指令进行斜率限制，避免速度突变
 *
 * 与电流斜坡类似，以 SPEED_REF_RAMP_RAD_S2 为最大变化率，
 * 逐步将生效的机械速度 (speed_ref_mech_applied_rad_s) 拉向目标值，
 * 防止阶跃速度指令引发的过流失控。最终将斜坡后的机械速度乘以极对数，
 * 得到电角速度给定 (speed_ref_elec_rad_s)。
 *
 * 调用时机：每个 FOC 快环周期调用一次
 */
static void program_update_speed_reference_ramp(void)
{
    float speed_step_rad_s = SPEED_REF_RAMP_RAD_S2 * FAST_LOOP_DT_S;
    float speed_delta = g_motor.speed_ref_mech_rad_s - g_motor.speed_ref_mech_applied_rad_s;
    speed_delta = program_clamp_f32(speed_delta, -speed_step_rad_s, speed_step_rad_s);
    g_motor.speed_ref_mech_applied_rad_s += speed_delta;
    g_motor.speed_ref_elec_rad_s = g_motor.speed_ref_mech_applied_rad_s * MOTOR_POLE_PAIRS;
}
```

### 补充3：速度量化保护

**功能**：在极低速时抑制编码器量化噪声导致的抖动。

```c
static float program_apply_speed_quantization_guard(float speed_mech_rad_s,
                                                    uint32_t observer_window_samples)
{
    /* 编码器 1 LSB 对应的速度分辨率 */
    float quantization_resolution = (6.28318530718f / 65536.0f)
        / (FAST_LOOP_DT_S * (float)observer_window_samples);

    float zero_hold_threshold = 8.0f * quantization_resolution;
    if (zero_hold_threshold < 0.35f) zero_hold_threshold = 0.35f;

    /* 速度指令和测速都在阈值以内 → 强制归零 */
    if ((fabsf(g_motor.speed_ref_mech_applied_rad_s) <= zero_hold_threshold) &&
        (fabsf(speed_mech_rad_s) <= zero_hold_threshold)) {
        return 0.0f;
    }
    return speed_mech_rad_s;
}
```

### 补充4：编码器观测器重归一化

**功能**：防止连续角无限增大导致浮点精度损失。

```c
static void program_renormalize_encoder_observer(void)
{
    if ((fabsf(g_encoder_continuous_mech_rad) < 32.0f * 6.28318530718f) &&
        (fabsf(g_encoder_speed_window_start_mech_rad) < 32.0f * 6.28318530718f)) {
        return;  /* 还没到需要归一的阈值 */
    }
    float anchor_rad = floorf(g_encoder_continuous_mech_rad / 6.28318530718f) * 6.28318530718f;
    g_encoder_continuous_mech_rad -= anchor_rad;
    g_encoder_speed_window_start_mech_rad -= anchor_rad;
}
```

### 补充5：位置环完整实现

**文件位置**：`program.c` 的 `program_update_position_loop()`、`program_reset_position_loop()`、`program_handle_position_loop_mode_switch()`

**架构**：位置环运行在 200Hz，输入目标角度(°)，输出机械速度指令，嵌套在速度环外部。

```
位置参考(°) ──→ PI(Kp=3.0) ──→ 速度指令 ──→ 速度环 ──→ 电流环
                                      ↑
编码器连续角(°) ──→ LPF(12Hz) ──────┘
```

#### 5.1 常量与变量

```c
#define PROGRAM_POSITION_LOOP_HZ              200.0f
#define PROGRAM_POSITION_LOOP_DT_S            (1.0f / PROGRAM_POSITION_LOOP_HZ)
#define PROGRAM_DEFAULT_POSITION_KP           3.0f
#define PROGRAM_DEFAULT_POSITION_KI           0.0f
#define PROGRAM_DEFAULT_POSITION_KD           0.0f
#define PROGRAM_POSITION_MEAS_LPF_CUTOFF_HZ   12.0f
#define PROGRAM_POSITION_HOLD_ERR_RAD         0.021f   /* ~1.2° 进入 hold */
#define PROGRAM_POSITION_HOLD_RELEASE_ERR_RAD 0.031f   /* ~1.8° 退出 hold */
#define PROGRAM_POSITION_HOLD_SPEED_MECH_RAD_S 0.50f
#define PROGRAM_POSITION_HOLD_RELEASE_CONFIRM_CYCLES 12U
#define PROGRAM_POSITION_CREEP_ENABLE_ERR_RAD 0.045f   /* ~2.6° 启动蠕动 */
#define PROGRAM_POSITION_CREEP_SPEED_MECH_RAD_S 0.020f

static float g_position_loop_elapsed_s = 0.0f;  /* 分频累计时间 */
static uint8_t g_position_hold_active = 0U;     /* 是否在 hold 状态 */
static uint8_t g_position_hold_release_counter = 0U;
static float g_position_meas_output_continuous_rad = 0.0f;
static filter_lpf_f32_t g_position_meas_lpf;    /* 位置测量 LPF */
```

#### 5.2 输出轴连续角获取

```c
/* 编码器装在转子上，通过减速比反算输出轴角度 */
static float program_get_encoder_output_continuous_mech_angle_rad(void)
{
    float rotor_mech = g_encoder_continuous_mech_rad;  /* 转子连续机械角 */
    return rotor_mech / MOTOR_GEAR_RATIO;              /* → 输出轴连续角 */
}
```

#### 5.3 分频调度（在速度环更新中调用）

在 `program_update_speed_loop()` 中，速度观测窗口触发时累加时间：

```c
/* speed_loop 内部 */
if (g_motor.position_loop_enable && g_speed_loop_update_pending) {
    g_position_loop_elapsed_s += g_speed_loop_dt_s;
    if (g_position_loop_elapsed_s >= PROGRAM_POSITION_LOOP_DT_S) {
        float pos_dt = g_position_loop_elapsed_s;
        g_position_loop_elapsed_s = 0.0f;
        program_update_position_loop(pos_dt);  /* 执行位置环 */
    }
}
```

#### 5.4 位置环核心算法

```c
static void program_update_position_loop(float position_loop_dt_s)
{
    if (!g_motor.position_loop_enable || !g_motor.speed_loop_enable) return;
    if (!g_encoder_speed_primed) return;

    /* ── 更新测量值 LPF ── */
    float raw_output_angle = program_get_encoder_output_continuous_mech_angle_rad();
    float filter_alpha = program_lpf_alpha_from_cutoff_hz(PROGRAM_POSITION_MEAS_LPF_CUTOFF_HZ, position_loop_dt_s);
    g_position_meas_lpf.alpha = filter_alpha;
    if (!g_position_meas_lpf.initialized)
        filter_lpf_f32_init(&g_position_meas_lpf, filter_alpha, raw_output_angle);
    g_position_meas_output_continuous_rad = filter_lpf_f32_update(&g_position_meas_lpf, raw_output_angle);

    /* ── 计算位置误差 ── */
    float ref_wrapped = program_deg_to_rad(program_wrap_angle_0_360_deg(g_motor.position_ref_mech_deg));
    float meas_wrapped = motor_params_wrap_angle_rad(g_position_meas_output_continuous_rad);
    float error_wrapped = program_wrap_delta_pm_pi(ref_wrapped - meas_wrapped);
    float error_abs = fabsf(error_wrapped);

    /* ── 滞回保持 ── */
    float output_speed = motor_params_rotor_speed_to_output_speed(g_motor.speed_meas_mech_rad_s);
    if ((error_abs <= PROGRAM_POSITION_HOLD_ERR_RAD) &&
        (fabsf(output_speed) <= PROGRAM_POSITION_HOLD_SPEED_MECH_RAD_S)) {
        g_position_hold_active = 1U;
        g_position_hold_release_counter = 0U;
        g_motor.position_integral_speed = 0.0f;
        g_motor.speed_ref_mech_rad_s = 0.0f;
        return;  /* hold：速度指令清零 */
    }

    /* 释放确认：必须连续 N 周期都超阈值才退出 hold */
    if (g_position_hold_active) {
        if (error_abs <= PROGRAM_POSITION_HOLD_RELEASE_ERR_RAD) {
            g_position_hold_release_counter = 0U;
            g_motor.speed_ref_mech_rad_s = 0.0f;
            return;
        }
        g_position_hold_release_counter++;
        if (g_position_hold_release_counter < PROGRAM_POSITION_HOLD_RELEASE_CONFIRM_CYCLES) {
            g_motor.speed_ref_mech_rad_s = 0.0f;
            return;
        }
        g_position_hold_active = 0U;
    }

    /* ── PI 计算 ── */
    float speed_cmd = program_run_pi_f32(error_wrapped, 0.0f,
        g_motor.position_kp, g_motor.position_ki, position_loop_dt_s,
        &g_motor.position_integral_speed,
        -g_motor.position_speed_limit_mech_rad_s,
        g_motor.position_speed_limit_mech_rad_s);
    speed_cmd -= g_motor.position_kd * output_speed;  /* 阻尼项 */

    /* ── 蠕动补偿：纯 P 控制在小误差时输出太小，强制给最小速度 ── */
    if ((error_abs > PROGRAM_POSITION_CREEP_ENABLE_ERR_RAD) &&
        (fabsf(speed_cmd) < PROGRAM_POSITION_CREEP_SPEED_MECH_RAD_S) &&
        (fabsf(output_speed) <= PROGRAM_POSITION_HOLD_SPEED_MECH_RAD_S)) {
        speed_cmd = copysignf(PROGRAM_POSITION_CREEP_SPEED_MECH_RAD_S, error_wrapped);
    }

    /* 输出轴速度 → 转子速度（× 减速比） */
    g_motor.speed_ref_mech_rad_s = speed_cmd * MOTOR_GEAR_RATIO;
}
```

#### 5.5 位置环模式切换

```c
static void program_handle_position_loop_mode_switch(void)
{
    uint8_t now = (g_motor.position_loop_enable != 0U) ? 1U : 0U;
    static uint8_t prev = 0U;
    if (now == prev) return;
    prev = now;

    program_reset_position_loop();
    program_reset_speed_loop();
    program_reset_speed_reference_ramp();

    if (now) {
        /* 启用时：用当前位置初始化目标位置，防止跳变 */
        g_position_meas_output_continuous_rad = program_get_encoder_output_continuous_mech_angle_rad();
        filter_lpf_f32_init(&g_position_meas_lpf,
            program_lpf_alpha_from_cutoff_hz(PROGRAM_POSITION_MEAS_LPF_CUTOFF_HZ, PROGRAM_POSITION_LOOP_DT_S),
            g_position_meas_output_continuous_rad);
        g_motor.position_ref_mech_deg = program_wrap_angle_0_360_deg(
            program_rad_to_deg(motor_params_wrap_angle_rad(g_position_meas_output_continuous_rad)));
    }
}
```

#### 5.6 位置环复位

```c
static void program_reset_position_loop(void)
{
    g_motor.position_integral_speed = 0.0f;
    g_motor.position_error_mech_deg = 0.0f;
    g_motor.position_error_mech_rad = 0.0f;
    g_position_loop_elapsed_s = 0.0f;
    g_position_hold_active = 0U;
    g_position_hold_release_counter = 0U;
    g_position_meas_output_continuous_rad = 0.0f;
    filter_lpf_f32_init(&g_position_meas_lpf,
        program_lpf_alpha_from_cutoff_hz(PROGRAM_POSITION_MEAS_LPF_CUTOFF_HZ, PROGRAM_POSITION_LOOP_DT_S), 0.0f);
}
```

#### 5.7 使用方法

```c
/* 启用位置环 */
g_motor.position_loop_enable = 1;
g_motor.position_kp = 3.0f;             /* P 增益 */
g_motor.position_speed_limit_mech_rad_s = 20.0f;  /* 输出速度限幅 */

/* 设定目标角度（输出轴） */
g_motor.position_ref_mech_deg = 90.0f;  /* 转到 90° 位置 */

/* 禁用位置环（回到速度模式） */
g_motor.position_loop_enable = 0;
```

#### 5.8 位置环测试

| 编号 | 功能点 | 测试方法 | 通过标准 |
|------|--------|---------|---------|
| P.1 | 位置跟踪 | 设 position_ref=90°，电机启动 | 电机转到 90° 并停止 |
| P.2 | hold 状态 | 电机到位后观察 `g_position_hold_active` | = 1，速度指令 = 0 |
| P.3 | 动态响应 | 修改 position_ref 从 90° → 180° | 电机平稳转到新位置 |
| P.4 | 手动偏移后恢复 | 到位后用手拧偏电机轴再松开 | 自动回到目标位置 |
| P.5 | 无超调/振荡 | 到位后观察编码器角度 | 不来回摆动，稳态误差 < 2° |

---

### 补充6：控制角度开环模式

**功能**：在不使用编码器反馈时，用积分器独立生成电角度，适用于调试和开环运行。

```c
static void program_update_control_angle_open_loop_state(void)
{
    if (!g_motor.control_angle_open_loop_enable || !g_encoder_align_done) return;
    g_motor.theta_open_loop = motor_params_wrap_angle_rad(
        g_motor.theta_open_loop +
        g_motor.control_angle_open_loop_speed_elec * FAST_LOOP_DT_S);
}
```

开关：`g_motor.control_angle_open_loop_enable = 1` 后，`program_get_control_elec_angle_rad()` 自动切换到积分器角度。

### 补充7：故障检测与状态管理

**功能**：实时监测驱动器故障引脚，更新全局故障状态。

```c
/* 读取 N_FAULT 引脚状态（低电平有效） */
static uint8_t program_is_driver_fault_active(void)
{
    return (HAL_GPIO_ReadPin(N_FAULT_GPIO_Port, N_FAULT_Pin) == GPIO_PIN_RESET) ? 1U : 0U;
}

/* 刷新故障标志到遥测结构体 */
static void program_update_fault_flags(void)
{
    g_program_telemetry.driver_fault_active = program_is_driver_fault_active();
}
```

### 补充8：安全复位函数

**功能**：在故障、停机或模式切换时，复位各控制环的积分器和状态，防止下次启动时过冲。

```c
/* 复位速度环 */
static void program_reset_speed_loop(void)
{
    g_motor.speed_integral_iq = 0.0f;
    g_motor.speed_integral_uq = 0.0f;
    g_motor.speed_ref_mech_applied_rad_s = 0.0f;
    g_motor.iq_ref = 0.0f;
    g_motor.uq_ref = 0.0f;
    g_motor.speed_meas_mech_rad_s = 0.0f;
    g_speed_pending = 0U;
}

/* 复位电流环 */
static void program_reset_current_loop(void)
{
    g_motor.id_integral_v = 0.0f;
    g_motor.iq_integral_v = 0.0f;
    g_motor.ud_ref = 0.0f;
    g_motor.uq_ref = 0.0f;
    g_id_applied = 0.0f;
    g_iq_applied = 0.0f;
}

/* 复位位置环 */
static void program_reset_position_loop(void)
{
    g_motor.position_integral_speed = 0.0f;
    g_motor.position_error_mech_deg = 0.0f;
    g_motor.position_error_mech_rad = 0.0f;
    g_position_loop_elapsed_s = 0.0f;
    g_position_hold_active = 0U;
    g_position_hold_release_counter = 0U;
    g_position_meas_output_continuous_rad = 0.0f;
}
```

### 补充9：遥测初始化与 DWT 周期计数器

**功能**：上电时初始化遥测结构体和 DWT 硬件计数器，用于快环耗时统计。

```c
/** 初始化遥测结构体 */
static void program_init_telemetry(void)
{
    memset((void *)&g_program_telemetry, 0, sizeof(g_program_telemetry));
    g_program_telemetry.ia_offset_raw = 2048U;
    g_program_telemetry.ib_offset_raw = 2048U;
    g_program_telemetry.ic_offset_raw = 2048U;
    g_program_telemetry.vbus = 48.0f;
}

/** 初始化 DWT 周期计数器（用于快环超时检测） */
static void program_init_cycle_counter(void)
{
    CoreDebug->DEMCR |= CoreDebug_DEMCR_TRCENA_Msk;
    DWT->CYCCNT = 0U;
    DWT->CTRL |= DWT_CTRL_CYCCNTENA_Msk;
    g_dwt_cycle_counter_ready = (DWT->CTRL & DWT_CTRL_CYCCNTENA_Msk) ? 1U : 0U;
    g_fast_loop_period_cycles = (SystemCoreClock != 0U) ? (uint32_t)((float)SystemCoreClock / 10000.0f) : 1U;
}

/** 更新快环耗时统计 */
static void program_update_fast_loop_timing(uint32_t elapsed_cycles)
{
    float elapsed_us = (float)elapsed_cycles / (float)SystemCoreClock * 1000000.0f;
    g_program_telemetry.fast_loop_cycles = elapsed_cycles;
    g_program_telemetry.fast_loop_time_us = elapsed_us;

    if (elapsed_cycles > g_program_telemetry.fast_loop_cycles_max) {
        g_program_telemetry.fast_loop_cycles_max = elapsed_cycles;
    }
    if (elapsed_us > g_program_telemetry.fast_loop_time_max_us) {
        g_program_telemetry.fast_loop_time_max_us = elapsed_us;
    }
    if (elapsed_cycles > g_fast_loop_period_cycles) {
        g_program_telemetry.fast_loop_overrun = 1U;
        g_program_telemetry.fast_loop_overrun_count++;
    } else {
        g_program_telemetry.fast_loop_overrun = 0U;
    }
}
```

### 补充10：VOFA 波形定时发送

**功能**：以固定周期（每 2ms）通过串口 DMA 发送电机运行波形数据到上位机 VOFA 软件。

```c
#define WAVE_PERIOD_MS 2U

static void program_send_wave_if_needed(uint32_t now_ms)
{
    if ((now_ms - g_last_wave_tick_ms) < WAVE_PERIOD_MS) return;
    g_last_wave_tick_ms = now_ms;

    float wave_data[] = {
        g_program_telemetry.id,
        g_program_telemetry.iq,
        g_program_telemetry.ia,
        g_program_telemetry.ib,
        g_program_telemetry.ic,
        g_program_telemetry.speed_meas_mech_rad_s,
        g_program_telemetry.speed_ref_mech_rad_s,
        g_program_telemetry.theta_elec
    };
    cli_uart_send_vofa(wave_data, sizeof(wave_data) / sizeof(wave_data[0]));
}
```

### 补充11：环模式切换函数

**功能**：检测位置环和电流环的使能状态变化，在切换时自动复位相关积分项和参考值。

```c
/* 位置环模式切换 */
static void program_handle_position_loop_mode_switch(void)
{
    uint8_t now = (g_motor.position_loop_enable != 0U) ? 1U : 0U;
    static uint8_t prev = 0U;
    if (now == prev) return;
    prev = now;

    program_reset_position_loop();
    program_reset_speed_loop();
    program_reset_speed_reference_ramp();

    if (now) {
        /* 启用位置环时，用当前位置初始化目标位置，防止跳变 */
        g_position_meas_output_continuous_rad = program_get_encoder_output_continuous_mech_angle_rad();
    }
}

/* 电流环模式切换 */
static void program_handle_current_loop_mode_switch(void)
{
    uint8_t now = (g_motor.current_loop_enable != 0U) ? 1U : 0U;
    static uint8_t prev = 1U;
    if (now == prev) return;
    prev = now;

    if (now) {
        /* 从电压模式切回电流模式时，预装积分项为当前电压命令 */
        g_motor.id_integral_v = program_clamp_f32(g_motor.ud_ref, -g_motor.voltage_limit, g_motor.voltage_limit);
        g_motor.iq_integral_v = program_clamp_f32(g_motor.uq_ref, -g_motor.voltage_limit, g_motor.voltage_limit);
    } else {
        program_reset_current_loop();
    }
}
```

---

## 补充：完整的功能对照表

| 功能 | 原始函数 | 文档位置 |
|------|---------|---------|
| 电压矢量限幅 | `program_limit_voltage_vector` | 补充1 |
| 电流参考斜坡 | `program_update_applied_current_references` | 补充2 |
| 速度参考斜坡 | `program_update_speed_reference_ramp` | 补充2 |
| 速度量化保护 | `program_apply_speed_quantization_guard` | 补充3 |
| 编码器重归一化 | `program_renormalize_encoder_observer` | 补充4 |
| 位置环核心 | `program_update_position_loop` | 补充5 |
| 位置环模式切换 | `program_handle_position_loop_mode_switch` | 补充5.5 |
| 位置环复位 | `program_reset_position_loop` | 补充5.6 |
| 控制角开环 | `program_update_control_angle_open_loop_state` | 补充6 |
| 故障检测 | `program_is_driver_fault_active` / `program_update_fault_flags` | 补充7 |
| 安全复位 | `program_reset_speed_loop` / `program_reset_current_loop` / `program_reset_position_loop` | 补充8 |
| 遥测 + DWT | `program_init_telemetry` / `program_init_cycle_counter` / `program_update_fast_loop_timing` | 补充9 |
| VOFA 发送 | `program_send_wave_if_needed` | 补充10 |
| 环模式切换 | `program_handle_position_loop_mode_switch` / `program_handle_current_loop_mode_switch` | 补充11 |
| CLI/VOFA 调试 | `cli_uart.c/h` | 详见源码 |
| CANopen 通信 | `fdcan.c/h` | 详见源码 |

---

---

## 最终文件结构

```
App/
  ├── program.h          ← 接口声明、结构体定义
  ├── program.c          ← 主逻辑（初始化+快环+慢环）
  └── ma600a.h/c         ← MA600A 编码器驱动
Core/Src/
  ├── main.c             ← 对接 program_init() / program_task()
  ├── adc.c/h            ← CubeMX 生成, ADC1/2 配置
  ├── tim.c/h            ← CubeMX 生成, TIM1/6 配置
  ├── spi.c/h            ← CubeMX 生成, SPI1 配置
  ├── dma.c/h            ← CubeMX 生成, DMA 配置
  ├── gpio.c/h           ← CubeMX 生成, 控制引脚配置
  └── usart.c/h          ← CubeMX 生成, USART1 配置(可选)
```

---

# 最终完整版：所有源代码文件

> 以下是与原项目完全一致的完整生产代码，按照文件建立顺序逐个创建即可复原整个程序。

---

## 文件清单（按依赖顺序创建）

```
App/
  ├── motor_params.h      ← ① 电机参数常量
  ├── filter.h            ← ② 低通滤波器
  ├── filter.c
  ├── drv_pid.h           ← ③ Q15 定点 PI 控制器
  ├── drv_pid.c
  ├── foc_core.h          ← ④ FOC 数学核心
  ├── foc_core.c
  ├── motor_state.h       ← ⑤ 状态机
  ├── motor_state.c
  ├── ma600a.h            ← ⑥ 编码器驱动
  ├── ma600a.c
  ├── program.h           ← ⑦ 顶层调度（头文件）
  └── program.c           ← ⑧ 顶层调度（实现）
```

---

## ① motor_params.h — 电机物理参数

```c
#ifndef MOTOR_PARAMS_H
#define MOTOR_PARAMS_H

#include <math.h>

#define MOTOR_POLE_PAIRS         14.0f
#define MOTOR_GEAR_RATIO         8.0f
#define MOTOR_ENCODER_ON_OUTPUT_SHAFT 0U
#define MOTOR_ENCODER_DIRECTION_SIGN (-1.0f)
#define MOTOR_TWO_PI             6.28318530718f

static inline float motor_params_wrap_angle_rad(float angle_rad)
{
    float turns;
    if (!isfinite(angle_rad)) return 0.0f;
    turns = floorf(angle_rad / MOTOR_TWO_PI);
    angle_rad -= turns * MOTOR_TWO_PI;
    if (angle_rad < 0.0f) angle_rad += MOTOR_TWO_PI;
    else if (angle_rad >= MOTOR_TWO_PI) angle_rad -= MOTOR_TWO_PI;
    return angle_rad;
}

static inline float motor_params_rotor_mech_to_elec_rad(float rotor_mech_angle_rad)
{
    return motor_params_wrap_angle_rad(rotor_mech_angle_rad * MOTOR_POLE_PAIRS);
}

static inline float motor_params_encoder_mech_to_rotor_mech_rad(float encoder_mech_angle_rad)
{
#if MOTOR_ENCODER_ON_OUTPUT_SHAFT
    return encoder_mech_angle_rad * MOTOR_ENCODER_DIRECTION_SIGN * MOTOR_GEAR_RATIO;
#else
    return encoder_mech_angle_rad * MOTOR_ENCODER_DIRECTION_SIGN;
#endif
}

static inline float motor_params_rotor_speed_to_output_speed(float rotor_speed_rad_s)
{
    return rotor_speed_rad_s / MOTOR_GEAR_RATIO;
}

#endif
```

---

## ② filter.h / filter.c — 一阶低通滤波器

### filter.h

```c
#ifndef FILTER_H
#define FILTER_H

#include <stdint.h>

typedef struct {
    float alpha;
    float value;
    uint8_t initialized;
} filter_lpf_f32_t;

typedef struct {
    int32_t alpha_q15;
    int32_t value_q15;
    uint8_t initialized;
} filter_lpf_s32_t;

void filter_lpf_f32_init(filter_lpf_f32_t *filter, float alpha, float initial_value);
float filter_lpf_f32_update(filter_lpf_f32_t *filter, float input);
void filter_lpf_s32_init(filter_lpf_s32_t *filter, int32_t alpha_q15, int32_t initial_value);
int32_t filter_lpf_s32_update(filter_lpf_s32_t *filter, int32_t input);

#endif
```

### filter.c

```c
#include "filter.h"

#define FILTER_Q15_ONE 32768L

static float filter_clamp_alpha(float alpha)
{
    if (alpha < 0.0f) return 0.0f;
    if (alpha > 1.0f) return 1.0f;
    return alpha;
}

static int32_t filter_clamp_q15(int32_t alpha_q15)
{
    if (alpha_q15 < 0) return 0;
    if (alpha_q15 > FILTER_Q15_ONE) return FILTER_Q15_ONE;
    return alpha_q15;
}

void filter_lpf_f32_init(filter_lpf_f32_t *filter, float alpha, float initial_value)
{
    if (filter == 0) return;
    filter->alpha = filter_clamp_alpha(alpha);
    filter->value = initial_value;
    filter->initialized = 1U;
}

float filter_lpf_f32_update(filter_lpf_f32_t *filter, float input)
{
    if (filter == 0) return input;
    if (filter->initialized == 0U) {
        filter_lpf_f32_init(filter, filter->alpha, input);
        return input;
    }
    filter->value += filter->alpha * (input - filter->value);
    return filter->value;
}

void filter_lpf_s32_init(filter_lpf_s32_t *filter, int32_t alpha_q15, int32_t initial_value)
{
    if (filter == 0) return;
    filter->alpha_q15 = filter_clamp_q15(alpha_q15);
    filter->value_q15 = initial_value << 15;
    filter->initialized = 1U;
}

int32_t filter_lpf_s32_update(filter_lpf_s32_t *filter, int32_t input)
{
    int32_t input_q15, delta_q15;
    if (filter == 0) return input;
    if (filter->initialized == 0U) {
        filter_lpf_s32_init(filter, filter->alpha_q15, input);
        return input;
    }
    input_q15 = input << 15;
    delta_q15 = input_q15 - filter->value_q15;
    filter->value_q15 += (int32_t)(((int64_t)filter->alpha_q15 * delta_q15) >> 15);
    return filter->value_q15 >> 15;
}
```

---

## ③ drv_pid.h / drv_pid.c — Q15 定点 PI 控制器

### drv_pid.h

```c
#ifndef _DRV_PID_
#define _DRV_PID_

#include <stdint.h>

typedef struct {
    int32_t ref, feedback, error;
    int32_t kp_q15, ki_q15;
    int32_t p_out, i_out, output;
    int32_t i_term_q15;
    int32_t out_min, out_max;
} drv_pid_pi_t;

void drv_pid_pi_init(drv_pid_pi_t *pid, int32_t kp_q15, int32_t ki_q15,
                     int32_t out_min, int32_t out_max, int32_t out_init);
void drv_pid_pi_reset(drv_pid_pi_t *pid, int32_t out_init);
int32_t drv_pid_pi_step(drv_pid_pi_t *pid, int32_t ref, int32_t feedback);

#endif
```

### drv_pid.c

```c
#include "drv_pid.h"

#define DRV_PID_Q15_SHIFT 15
#define DRV_PID_Q15_HALF  (1L << (DRV_PID_Q15_SHIFT - 1))

static int32_t drv_pid_clamp_s32(int32_t value, int32_t min_value, int32_t max_value)
{
    if (value < min_value) return min_value;
    if (value > max_value) return max_value;
    return value;
}

static int32_t drv_pid_s32_to_q15(int32_t value) { return value << DRV_PID_Q15_SHIFT; }

static int32_t drv_pid_q15_to_s32_round(int32_t value_q15)
{
    if (value_q15 >= 0) return (value_q15 + DRV_PID_Q15_HALF) >> DRV_PID_Q15_SHIFT;
    return -(((-value_q15) + DRV_PID_Q15_HALF) >> DRV_PID_Q15_SHIFT);
}

void drv_pid_pi_init(drv_pid_pi_t *pid, int32_t kp_q15, int32_t ki_q15,
                     int32_t out_min, int32_t out_max, int32_t out_init)
{
    if (pid == 0) return;
    pid->kp_q15 = kp_q15;
    pid->ki_q15 = ki_q15;
    pid->out_min = out_min;
    pid->out_max = out_max;
    pid->ref = 0;
    pid->feedback = 0;
    pid->error = 0;
    pid->p_out = 0;
    drv_pid_pi_reset(pid, out_init);
}

void drv_pid_pi_reset(drv_pid_pi_t *pid, int32_t out_init)
{
    if (pid == 0) return;
    out_init = drv_pid_clamp_s32(out_init, pid->out_min, pid->out_max);
    pid->ref = 0;
    pid->feedback = 0;
    pid->error = 0;
    pid->p_out = 0;
    pid->output = out_init;
    pid->i_out = out_init;
    pid->i_term_q15 = drv_pid_s32_to_q15(out_init);
}

int32_t drv_pid_pi_step(drv_pid_pi_t *pid, int32_t ref, int32_t feedback)
{
    int32_t p_term_q15, i_candidate_q15, out_q15, out_s32;
    if (pid == 0) return 0;
    pid->ref = ref;
    pid->feedback = feedback;
    pid->error = ref - feedback;

    p_term_q15 = pid->error * pid->kp_q15;
    i_candidate_q15 = pid->i_term_q15 + (pid->error * pid->ki_q15);
    i_candidate_q15 = drv_pid_clamp_s32(i_candidate_q15,
        drv_pid_s32_to_q15(pid->out_min), drv_pid_s32_to_q15(pid->out_max));

    out_q15 = p_term_q15 + i_candidate_q15;
    out_s32 = drv_pid_q15_to_s32_round(out_q15);

    if (out_s32 > pid->out_max) {
        out_s32 = pid->out_max;
        if (pid->error < 0) pid->i_term_q15 = i_candidate_q15;
    } else if (out_s32 < pid->out_min) {
        out_s32 = pid->out_min;
        if (pid->error > 0) pid->i_term_q15 = i_candidate_q15;
    } else {
        pid->i_term_q15 = i_candidate_q15;
    }
    pid->p_out = drv_pid_q15_to_s32_round(p_term_q15);
    pid->i_out = drv_pid_q15_to_s32_round(pid->i_term_q15);
    pid->output = out_s32;
    return out_s32;
}
```

---

## ④ foc_core.h / foc_core.c — FOC 数学核心

### foc_core.h

```c
#ifndef FOC_CORE_H
#define FOC_CORE_H

typedef struct { float alpha; float beta; } foc_alpha_beta_t;
typedef struct { float d; float q; } foc_dq_t;
typedef struct { float duty_a; float duty_b; float duty_c; } foc_svpwm_duty_t;

typedef struct {
    float vbus, theta_elec, sin_theta, cos_theta;
    foc_alpha_beta_t i_ab;
    foc_dq_t i_dq, v_dq_cmd;
    foc_alpha_beta_t v_ab_cmd;
    foc_svpwm_duty_t duty;
} foc_core_t;

void foc_core_init(foc_core_t *core);
void foc_core_reset_output(foc_core_t *core);
void foc_core_set_bus_voltage(foc_core_t *core, float vbus);
void foc_core_set_electrical_angle(foc_core_t *core, float theta_elec);
void foc_core_clarke(float ia, float ib, foc_alpha_beta_t *out);
void foc_core_park(const foc_alpha_beta_t *ab, float sin_theta, float cos_theta, foc_dq_t *out);
void foc_core_inv_park(const foc_dq_t *dq, float sin_theta, float cos_theta, foc_alpha_beta_t *out);
void foc_core_svpwm(foc_core_t *core, float v_alpha, float v_beta, float vbus);
void foc_core_run_voltage_open_loop(foc_core_t *core, float ud, float uq, float theta_elec, float vbus);

#endif
```

### foc_core.c

```c
#include "foc_core.h"
#include <math.h>

#define FOC_TWO_PI    6.28318530718f
#define FOC_SQRT3     1.73205080757f
#define FOC_INV_SQRT3 0.57735026919f

static float clamp_f(float v, float lo, float hi)
{ if (v < lo) return lo; if (v > hi) return hi; return v; }

static float wrap_angle(float theta)
{
    if (!isfinite(theta)) return 0.0f;
    float t = floorf(theta / FOC_TWO_PI);
    theta -= t * FOC_TWO_PI;
    if (theta < 0.0f) theta += FOC_TWO_PI;
    else if (theta >= FOC_TWO_PI) theta -= FOC_TWO_PI;
    return theta;
}

void foc_core_init(foc_core_t *core)
{
    if (core == 0) return;
    core->vbus = 24.0f;
    core->theta_elec = 0.0f;
    core->sin_theta = 0.0f;
    core->cos_theta = 1.0f;
    core->i_ab.alpha = core->i_ab.beta = 0.0f;
    core->i_dq.d = core->i_dq.q = 0.0f;
    core->v_dq_cmd.d = core->v_dq_cmd.q = 0.0f;
    core->v_ab_cmd.alpha = core->v_ab_cmd.beta = 0.0f;
    core->duty.duty_a = core->duty.duty_b = core->duty.duty_c = 0.5f;
}

void foc_core_reset_output(foc_core_t *core)
{
    if (core == 0) return;
    core->v_dq_cmd.d = core->v_dq_cmd.q = 0.0f;
    core->v_ab_cmd.alpha = core->v_ab_cmd.beta = 0.0f;
    core->duty.duty_a = core->duty.duty_b = core->duty.duty_c = 0.5f;
}

void foc_core_set_bus_voltage(foc_core_t *core, float vbus)
{ if (core) core->vbus = vbus > 1.0f ? vbus : 1.0f; }

void foc_core_set_electrical_angle(foc_core_t *core, float theta_elec)
{
    if (core == 0) return;
    core->theta_elec = wrap_angle(theta_elec);
    core->sin_theta = sinf(core->theta_elec);
    core->cos_theta = cosf(core->theta_elec);
}

void foc_core_clarke(float ia, float ib, foc_alpha_beta_t *out)
{
    if (out == 0) return;
    out->alpha = ia;
    out->beta = (ia + 2.0f * ib) * FOC_INV_SQRT3;
}

void foc_core_park(const foc_alpha_beta_t *ab, float sin_t, float cos_t, foc_dq_t *out)
{
    if ((ab == 0) || (out == 0)) return;
    out->d = ab->alpha * cos_t + ab->beta * sin_t;
    out->q = -ab->alpha * sin_t + ab->beta * cos_t;
}

void foc_core_inv_park(const foc_dq_t *dq, float sin_t, float cos_t, foc_alpha_beta_t *out)
{
    if ((dq == 0) || (out == 0)) return;
    out->alpha = dq->d * cos_t - dq->q * sin_t;
    out->beta  = dq->d * sin_t + dq->q * cos_t;
}

void foc_core_svpwm(foc_core_t *core, float v_alpha, float v_beta, float vbus)
{
    if (core == 0) return;
    if (vbus < 1.0f) {
        core->duty.duty_a = core->duty.duty_b = core->duty.duty_c = 0.5f;
        return;
    }
    float va = v_alpha / vbus;
    float vb = (-0.5f * v_alpha + 0.5f * FOC_SQRT3 * v_beta) / vbus;
    float vc = (-0.5f * v_alpha - 0.5f * FOC_SQRT3 * v_beta) / vbus;
    float vmax = va; if (vb > vmax) vmax = vb; if (vc > vmax) vmax = vc;
    float vmin = va; if (vb < vmin) vmin = vb; if (vc < vmin) vmin = vc;
    float offset = 0.5f - 0.5f * (vmax + vmin);
    core->duty.duty_a = clamp_f(va + offset, 0.0f, 1.0f);
    core->duty.duty_b = clamp_f(vb + offset, 0.0f, 1.0f);
    core->duty.duty_c = clamp_f(vc + offset, 0.0f, 1.0f);
}

void foc_core_run_voltage_open_loop(foc_core_t *core, float ud, float uq, float theta_elec, float vbus)
{
    if (core == 0) return;
    foc_core_set_bus_voltage(core, vbus);
    foc_core_set_electrical_angle(core, theta_elec);
    core->v_dq_cmd.d = ud;
    core->v_dq_cmd.q = uq;
    foc_core_inv_park(&core->v_dq_cmd, core->sin_theta, core->cos_theta, &core->v_ab_cmd);
    foc_core_svpwm(core, core->v_ab_cmd.alpha, core->v_ab_cmd.beta, core->vbus);
}
```

---

## ⑤ motor_state.h / motor_state.c — 状态机

### motor_state.h

```c
#ifndef MOTOR_STATE_H
#define MOTOR_STATE_H

#include <stdint.h>
#include "drv_pid.h"
#include "filter.h"
#include "foc_core.h"

typedef enum {
    MOTOR_STATE_INIT = 0,
    MOTOR_STATE_READY,
    MOTOR_STATE_ALIGN,
    MOTOR_STATE_OPEN_LOOP,
    MOTOR_STATE_CLOSED_LOOP,
    MOTOR_STATE_FAULT
} motor_state_id_t;

typedef enum {
    MOTOR_FAULT_NONE = 0,
    MOTOR_FAULT_DRIVER = 1,
    MOTOR_FAULT_OVERVOLTAGE = 2
} motor_fault_t;

typedef struct {
    motor_state_id_t state;
    uint8_t run_request, speed_loop_enable, current_loop_enable, position_loop_enable;
    uint8_t control_angle_open_loop_enable, align_done, fault_code;
    uint32_t state_enter_ms;
    float theta_open_loop, open_loop_speed_elec, control_angle_open_loop_speed_elec;
    float id_ref, iq_ref, ud_ref, uq_ref;
    float speed_ref_mech_rpm, speed_ref_mech_rad_s, speed_ref_mech_applied_rad_s;
    float speed_meas_mech_rad_s, speed_ref_elec_rad_s, speed_meas_elec_rad_s;
    float speed_kp, speed_ki;
    float position_ref_mech_deg, position_ref_mech_rad;
    float position_meas_mech_deg, position_meas_mech_rad;
    float position_error_mech_deg, position_error_mech_rad;
    float position_kp, position_ki, position_kd;
    float position_integral_speed, position_speed_limit_mech_rad_s;
    float speed_meas_lpf_cutoff_hz;
    float speed_integral_iq, speed_integral_uq;
    float iq_limit, current_kp, current_ki;
    float id_integral_v, iq_integral_v, voltage_limit;
    filter_lpf_f32_t speed_lpf;
    drv_pid_pi_t speed_pi;
} motor_state_t;

void motor_state_init(motor_state_t *motor);
void motor_state_task(motor_state_t *motor, foc_core_t *foc, uint32_t now_ms);
void motor_state_set_run_request(motor_state_t *motor, uint8_t enable);
void motor_state_set_fault(motor_state_t *motor, uint8_t fault_code);
void motor_state_clear_fault(motor_state_t *motor);
const char *motor_state_get_name(motor_state_id_t state);

#endif
```

### motor_state.c

```c
#include "motor_state.h"

#define MOTOR_ALIGN_TIME_MS           300U
#define MOTOR_OPEN_LOOP_TO_CLOSE_MS   1500U
#define MOTOR_OPEN_LOOP_SPEED_ELEC    20.0f
#define MOTOR_ALIGN_UD_V              0.8f
#define MOTOR_OPEN_LOOP_UQ_V          1.0f

static void motor_state_enter(motor_state_t *motor, motor_state_id_t next, uint32_t now_ms)
{ motor->state = next; motor->state_enter_ms = now_ms; }

void motor_state_init(motor_state_t *motor)
{
    if (motor == 0) return;
    motor->state = MOTOR_STATE_READY;
    motor->run_request = 0U;
    motor->speed_loop_enable = 1U;
    motor->current_loop_enable = 1U;
    motor->position_loop_enable = 0U;
    motor->control_angle_open_loop_enable = 0U;
    motor->align_done = 0U;
    motor->fault_code = MOTOR_FAULT_NONE;
    motor->state_enter_ms = 0U;
    motor->theta_open_loop = 0.0f;
    motor->open_loop_speed_elec = MOTOR_OPEN_LOOP_SPEED_ELEC;
    motor->control_angle_open_loop_speed_elec = MOTOR_OPEN_LOOP_SPEED_ELEC;
    motor->id_ref = motor->iq_ref = motor->ud_ref = motor->uq_ref = 0.0f;
    motor->speed_ref_mech_rpm = motor->speed_ref_mech_rad_s = 0.0f;
    motor->speed_ref_mech_applied_rad_s = motor->speed_meas_mech_rad_s = 0.0f;
    motor->speed_ref_elec_rad_s = motor->speed_meas_elec_rad_s = 0.0f;
    motor->speed_kp = 0.001f; motor->speed_ki = 0.004f;
    motor->position_ref_mech_deg = motor->position_ref_mech_rad = 0.0f;
    motor->position_meas_mech_deg = motor->position_meas_mech_rad = 0.0f;
    motor->position_error_mech_deg = motor->position_error_mech_rad = 0.0f;
    motor->position_kp = 8.0f; motor->position_ki = 0.0f; motor->position_kd = 0.0f;
    motor->position_integral_speed = 0.0f; motor->position_speed_limit_mech_rad_s = 20.0f;
    motor->speed_meas_lpf_cutoff_hz = 150.0f;
    motor->speed_integral_iq = motor->speed_integral_uq = 0.0f;
    motor->iq_limit = 2.0f; motor->current_kp = 0.5f; motor->current_ki = 200.0f;
    motor->id_integral_v = motor->iq_integral_v = motor->voltage_limit = 0.0f;
    filter_lpf_f32_init(&motor->speed_lpf, 0.1f, 0.0f);
    drv_pid_pi_init(&motor->speed_pi, 0, 0, -1000, 1000, 0);
}

void motor_state_task(motor_state_t *motor, foc_core_t *foc, uint32_t now_ms)
{
    if ((motor == 0) || (foc == 0)) return;
    if (motor->fault_code != MOTOR_FAULT_NONE)
        motor_state_enter(motor, MOTOR_STATE_FAULT, now_ms);

    switch (motor->state) {
    case MOTOR_STATE_READY:
        foc_core_reset_output(foc);
        if (motor->run_request) { motor->align_done = 0U; motor_state_enter(motor, MOTOR_STATE_ALIGN, now_ms); }
        break;
    case MOTOR_STATE_ALIGN:
        motor->theta_open_loop = 0.0f; motor->ud_ref = MOTOR_ALIGN_UD_V; motor->uq_ref = 0.0f;
        foc_core_run_voltage_open_loop(foc, motor->ud_ref, motor->uq_ref, motor->theta_open_loop, foc->vbus);
        if ((now_ms - motor->state_enter_ms) >= MOTOR_ALIGN_TIME_MS)
            { motor->align_done = 1U; motor_state_enter(motor, MOTOR_STATE_OPEN_LOOP, now_ms); }
        break;
    case MOTOR_STATE_OPEN_LOOP:
        motor->theta_open_loop = 0.001f * (float)(now_ms - motor->state_enter_ms) * motor->open_loop_speed_elec;
        motor->ud_ref = 0.0f; motor->uq_ref = MOTOR_OPEN_LOOP_UQ_V;
        foc_core_run_voltage_open_loop(foc, motor->ud_ref, motor->uq_ref, motor->theta_open_loop, foc->vbus);
        if (!motor->run_request) motor_state_enter(motor, MOTOR_STATE_READY, now_ms);
        else if ((now_ms - motor->state_enter_ms) >= MOTOR_OPEN_LOOP_TO_CLOSE_MS)
            motor_state_enter(motor, MOTOR_STATE_CLOSED_LOOP, now_ms);
        break;
    case MOTOR_STATE_CLOSED_LOOP:
        motor->theta_open_loop += 0.001f * motor->open_loop_speed_elec;
        motor->ud_ref = 0.0f; motor->uq_ref = MOTOR_OPEN_LOOP_UQ_V;
        foc_core_run_voltage_open_loop(foc, motor->ud_ref, motor->uq_ref, motor->theta_open_loop, foc->vbus);
        if (!motor->run_request) motor_state_enter(motor, MOTOR_STATE_READY, now_ms);
        break;
    case MOTOR_STATE_FAULT:
        foc_core_reset_output(foc);
        if (!motor->run_request && motor->fault_code == MOTOR_FAULT_NONE)
            motor_state_enter(motor, MOTOR_STATE_READY, now_ms);
        break;
    default:
        motor_state_enter(motor, MOTOR_STATE_READY, now_ms); break;
    }
}

void motor_state_set_run_request(motor_state_t *motor, uint8_t enable) { if (motor) motor->run_request = enable; }
void motor_state_set_fault(motor_state_t *motor, uint8_t code) { if (motor) motor->fault_code = code; }
void motor_state_clear_fault(motor_state_t *motor) { if (motor) motor->fault_code = MOTOR_FAULT_NONE; }

const char *motor_state_get_name(motor_state_id_t state)
{
    switch (state) {
    case MOTOR_STATE_INIT: return "INIT";
    case MOTOR_STATE_READY: return "READY";
    case MOTOR_STATE_ALIGN: return "ALIGN";
    case MOTOR_STATE_OPEN_LOOP: return "OPEN_LOOP";
    case MOTOR_STATE_CLOSED_LOOP: return "CLOSED_LOOP";
    case MOTOR_STATE_FAULT: return "FAULT";
    default: return "UNKNOWN";
    }
}
```

---

## ⑥ ma600a.h / ma600a.c — MA600A 磁编码器驱动

### ma600a.h

```c
#ifndef MA600A_H
#define MA600A_H

#include <stdint.h>
#include "spi.h"

typedef struct {
    SPI_HandleTypeDef *hspi;
    GPIO_TypeDef *cs_port;
    uint16_t cs_pin, tx_word, rx_word, angle_raw;
    float angle_deg, angle_rad;
    uint8_t data_valid, transfer_busy, consecutive_bad_count;
    uint32_t sample_counter, reject_count, comm_error_count;
} ma600a_t;

void ma600a_init(ma600a_t *sensor, SPI_HandleTypeDef *hspi, GPIO_TypeDef *cs_port, uint16_t cs_pin);
uint8_t ma600a_read_angle(ma600a_t *sensor);
void ma600a_spi_txrx_cplt_callback(ma600a_t *sensor, SPI_HandleTypeDef *hspi);
void ma600a_spi_error_callback(ma600a_t *sensor, SPI_HandleTypeDef *hspi);

#endif
```

### ma600a.c

```c
#include "ma600a.h"

#define MA600A_FULL_SCALE_COUNTS_F   65536.0f
#define MA600A_HALF_SCALE_COUNTS     32768L
#define MA600A_ANGLE_CMD             0x0000U
#define MA600A_TWO_PI                6.28318530718f
#define MA600A_MAX_JUMP_COUNTS       1024L
#define MA600A_MAX_BAD_SAMPLES       4U

static void cs_low(const ma600a_t *s)  { HAL_GPIO_WritePin(s->cs_port, s->cs_pin, GPIO_PIN_RESET); }
static void cs_high(const ma600a_t *s) { HAL_GPIO_WritePin(s->cs_port, s->cs_pin, GPIO_PIN_SET); }

static void update_angle(ma600a_t *s, uint16_t raw)
{
    s->angle_raw = raw;
    s->angle_deg = (float)raw * 360.0f / MA600A_FULL_SCALE_COUNTS_F;
    s->angle_rad = (float)raw * MA600A_TWO_PI / MA600A_FULL_SCALE_COUNTS_F;
    s->data_valid = 1U;
    s->consecutive_bad_count = 0U;
    s->sample_counter++;
}

static int32_t delta_counts(uint16_t cur, uint16_t prev)
{
    int32_t d = (int32_t)cur - (int32_t)prev;
    if (d > MA600A_HALF_SCALE_COUNTS) d -= 65536;
    else if (d < -MA600A_HALF_SCALE_COUNTS) d += 65536;
    return d;
}

static uint8_t is_plausible(const ma600a_t *s, uint16_t raw)
{
    if (!s || !s->data_valid || s->sample_counter == 0U) return 1U;
    int32_t d = delta_counts(raw, s->angle_raw);
    return (d <= MA600A_MAX_JUMP_COUNTS && d >= -MA600A_MAX_JUMP_COUNTS) ? 1U : 0U;
}

static void note_bad(ma600a_t *s, uint8_t comm_err)
{
    if (!s) return;
    s->rx_word = 0U;
    if (comm_err) s->comm_error_count++; else s->reject_count++;
    if (s->consecutive_bad_count < UINT8_MAX) s->consecutive_bad_count++;
    if (s->consecutive_bad_count >= MA600A_MAX_BAD_SAMPLES) s->data_valid = 0U;
}

void ma600a_init(ma600a_t *sensor, SPI_HandleTypeDef *hspi, GPIO_TypeDef *cs_port, uint16_t cs_pin)
{
    if (!sensor || !hspi || !cs_port) return;
    sensor->hspi = hspi; sensor->cs_port = cs_port; sensor->cs_pin = cs_pin;
    sensor->tx_word = sensor->rx_word = sensor->angle_raw = 0U;
    sensor->angle_deg = sensor->angle_rad = 0.0f;
    sensor->data_valid = sensor->transfer_busy = sensor->consecutive_bad_count = 0U;
    sensor->sample_counter = sensor->reject_count = sensor->comm_error_count = 0U;
    cs_high(sensor);
}

uint8_t ma600a_read_angle(ma600a_t *sensor)
{
    if (!sensor || !sensor->hspi || !sensor->cs_port) return 0U;
    if (sensor->transfer_busy) return 0U;
    sensor->tx_word = MA600A_ANGLE_CMD;
    sensor->rx_word = 0U;
    cs_low(sensor);
    HAL_StatusTypeDef st = HAL_SPI_TransmitReceive_IT(sensor->hspi,
        (uint8_t *)&sensor->tx_word, (uint8_t *)&sensor->rx_word, 1U);
    if (st != HAL_OK) {
        cs_high(sensor);
        if (st != HAL_BUSY) note_bad(sensor, 1U);
        return 0U;
    }
    sensor->transfer_busy = 1U;
    return 1U;
}

void ma600a_spi_txrx_cplt_callback(ma600a_t *sensor, SPI_HandleTypeDef *hspi)
{
    if (!sensor || !hspi || sensor->hspi != hspi) return;
    cs_high(sensor);
    sensor->transfer_busy = 0U;
    if (!is_plausible(sensor, sensor->rx_word)) { note_bad(sensor, 0U); return; }
    update_angle(sensor, sensor->rx_word);
}

void ma600a_spi_error_callback(ma600a_t *sensor, SPI_HandleTypeDef *hspi)
{
    if (!sensor || !hspi || sensor->hspi != hspi) return;
    cs_high(sensor);
    sensor->transfer_busy = 0U;
    note_bad(sensor, 1U);
}
```

---

## ⑦ program.h — 顶层调度头文件

```c
#ifndef PROGRAM_H
#define PROGRAM_H

#include "main.h"
#include "adc.h"
#include "tim.h"
#include "usart.h"
#include "motor_state.h"
#include "foc_core.h"

typedef struct {
    uint16_t ia_raw, ib_raw, ic_raw, vbus_raw, ntc_raw;
    uint16_t ma600a_angle_raw;
    uint16_t ia_offset_raw, ib_offset_raw, ic_offset_raw;
    uint16_t current_offset_sample_count;
    uint8_t  current_offset_ready, pwm_enable_cmd, power_stage_enabled;
    uint8_t  control_state, encoder_align_done, speed_loop_ready;
    uint8_t  current_loop_enable, position_loop_enable, control_angle_open_loop_enable;
    uint8_t  driver_fault_active, ma600a_angle_valid, ma600a_consecutive_bad_count;
    uint8_t  fast_loop_overrun;
    uint32_t fast_loop_overrun_count, fast_loop_cycles, fast_loop_cycles_max;
    uint32_t ma600a_sample_counter, ma600a_reject_count, ma600a_comm_error_count;
    uint32_t speed_observer_window_samples;
    float ia, ib, ic, ic_meas, i_abc_sum, id, iq, theta_elec;
    float duty_a, duty_b, duty_c, vbus, ma600a_angle_deg, ma600a_angle_rad;
    float theta_open_loop, control_angle_open_loop_speed_elec;
    float id_ref_cmd, iq_ref_cmd, id_ref_applied_cmd, iq_ref_applied_cmd;
    float ud_ref_cmd, uq_ref_cmd, open_loop_speed_elec;
    float speed_ref_mech_rad_s, speed_ref_mech_applied_rad_s, speed_meas_raw_mech_rad_s;
    float speed_meas_mech_rad_s, speed_ref_mech_rpm, speed_ref_mech_applied_rpm;
    float speed_meas_raw_mech_rpm, speed_meas_mech_rpm, speed_error_mech_rad_s;
    float position_ref_mech_rad, position_meas_mech_rad, position_error_mech_rad;
    float position_ref_mech_deg, position_meas_mech_deg, position_error_mech_deg;
    float position_kd, speed_loop_dt_s, speed_meas_lpf_cutoff_hz;
    float speed_ref_elec_rad_s, speed_meas_elec_rad_s;
    float uq_limit_v, iq_limit_a, voltage_limit_v, encoder_elec_offset_rad;
    float fast_loop_time_us, fast_loop_time_max_us, fast_loop_period_us;
} program_telemetry_t;

typedef struct {
    uint8_t enable;
    float duty_a, duty_b, duty_c;
} program_debug_pwm_test_t;

extern volatile program_telemetry_t g_program_telemetry;
extern volatile program_debug_pwm_test_t g_program_debug_pwm_test;
extern motor_state_t g_motor;
extern foc_core_t g_foc;

void program_init(void);
void program_task(void);
void program_tim_period_elapsed_callback(TIM_HandleTypeDef *htim);
void program_adc_conv_cplt_callback(ADC_HandleTypeDef *hadc);
void program_adc_injected_conv_cplt_callback(ADC_HandleTypeDef *hadc);
const volatile program_telemetry_t *program_get_telemetry(void);
motor_state_t *program_get_motor(void);
foc_core_t *program_get_foc(void);

#endif
```

---

## ⑧ program.c — 顶层调度实现（完整版）

```c
#include "program.h"
#include <math.h>
#include <string.h>
#include "gpio.h"
#include "ma600a.h"
#include "motor_params.h"

/* ── 硬件参数常量 ── */
#define ADC_REF_V                     3.3f
#define ADC_FULL_SCALE                4095.0f
#define SHUNT_RESISTOR_OHM            0.01f
#define CURRENT_SENSE_GAIN            20.0f
#define VBUS_R_UP_OHM                 240000.0f
#define VBUS_R_DOWN_OHM               10000.0f
#define DEFAULT_CURRENT_OFFSET_RAW    2048U
#define CURRENT_OFFSET_TARGET_SAMPLES 1024U
#define DEFAULT_VBUS_V                48.0f
#define PI                            3.14159265359f
#define FAST_LOOP_HZ                  10000.0f
#define FAST_LOOP_DT_S                (1.0f / FAST_LOOP_HZ)
#define SPEED_OBSERVER_WINDOW         20U
#define SPEED_MEAS_LPF_CUTOFF_HZ      150.0f
#define ALIGN_UD_V                    1.8f
#define ALIGN_HOLD_TICKS              8000U
#define ALIGN_SAMPLE_WINDOW_TICKS     512U
#define WAVE_PERIOD_MS                2U
#define ADC2_DMA_LENGTH               2U

/* ── 全局对象 ── */
motor_state_t g_motor;
foc_core_t g_foc;
static ma600a_t g_ma600a;
volatile program_telemetry_t g_program_telemetry;
volatile program_debug_pwm_test_t g_program_debug_pwm_test;

/* ── 内部状态 ── */
static volatile uint16_t g_adc2_dma_buf[ADC2_DMA_LENGTH];
static volatile uint32_t g_tim6_tick_ms = 0U;
static volatile uint32_t g_ia_offs_sum = 0U, g_ib_offs_sum = 0U, g_ic_offs_sum = 0U;
static uint32_t g_last_slow_ms = 0U, g_last_wave_ms = 0U;
static uint8_t g_pwm_started = 0U, g_power_enabled = 0U;

/* ── 编码器观测器 ── */
static uint32_t g_enc_last_cnt = 0U;
static uint8_t g_enc_primed = 0U, g_enc_speed_ok = 0U, g_speed_pending = 0U;
static float g_enc_prev_mech = 0.0f, g_enc_cont_mech = 0.0f;
static float g_enc_ws_start = 0.0f;
static uint32_t g_enc_ws_cnt = 0U;
static float g_enc_speed_raw = 0.0f;
static uint8_t g_align_done = 0U;
static uint32_t g_align_cnt = 0U;
static float g_enc_offset = 0.0f;
static float g_align_sin_sum = 0.0f, g_align_cos_sum = 0.0f;
static uint32_t g_align_sample_cnt = 0U;

/* ── 控制状态 ── */
static float g_id_applied = 0.0f, g_iq_applied = 0.0f;
static filter_lpf_f32_t g_vbus_lpf, g_speed_lpf;
static float g_speed_loop_dt = (FAST_LOOP_DT_S * SPEED_OBSERVER_WINDOW);
static uint8_t g_pos_en_prev = 0U, g_cur_en_prev = 1U;
static uint8_t g_dwt_ready = 0U;
static uint32_t g_fl_period_cyc = 1U;

/* ── 工具函数 ── */
static float program_clamp_f32(float v, float lo, float hi)
{ if (v < lo) return lo; if (v > hi) return hi; return v; }

static float program_wrap_angle_0_2pi(float a)
{
    if (!isfinite(a)) return 0.0f;
    float t = floorf(a / (2.0f * PI));
    a -= t * 2.0f * PI;
    if (a < 0.0f) a += 2.0f * PI;
    return a;
}

static float program_wrap_delta_pm_pi(float a)
{
    if (!isfinite(a)) return 0.0f;
    return program_wrap_angle_0_2pi(a + PI) - PI;
}

static float program_rad_s_to_rpm(float r) { return r * (60.0f / (2.0f * PI)); }
static float program_rpm_to_rad_s(float r) { return r * ((2.0f * PI) / 60.0f); }
static float program_rad_to_deg(float r) { return r * (360.0f / (2.0f * PI)); }
static float program_deg_to_rad(float d) { return d * ((2.0f * PI) / 360.0f); }

static float program_lpf_alpha_from_cutoff_hz(float fc, float dt)
{
    if (fc <= 0.0f || dt <= 0.0f) return 1.0f;
    return program_clamp_f32(1.0f - expf(-2.0f * PI * fc * dt), 0.0f, 1.0f);
}

static float program_convert_current_from_raw(uint16_t raw, uint16_t offs)
{ return ((float)raw - (float)offs) * ADC_REF_V / ADC_FULL_SCALE / (CURRENT_SENSE_GAIN * SHUNT_RESISTOR_OHM); }

static float program_convert_vbus_from_raw(uint16_t raw)
{ return (float)raw * ADC_REF_V / ADC_FULL_SCALE * ((VBUS_R_UP_OHM + VBUS_R_DOWN_OHM) / VBUS_R_DOWN_OHM); }

static float program_get_encoder_rotor_mech_angle_rad(void)
{ return motor_params_encoder_mech_to_rotor_mech_rad(g_ma600a.angle_rad); }

static float program_get_encoder_raw_elec_angle_rad(void)
{ return motor_params_rotor_mech_to_elec_rad(program_get_encoder_rotor_mech_angle_rad()); }

static float program_get_encoder_aligned_elec_angle_rad(void)
{ return motor_params_wrap_angle_rad(program_get_encoder_raw_elec_angle_rad() - g_enc_offset); }

static float program_get_control_elec_angle_rad(void)
{
    if (g_motor.control_angle_open_loop_enable)
        return motor_params_wrap_angle_rad(g_motor.theta_open_loop);
    return program_get_encoder_aligned_elec_angle_rad();
}

/* ── 浮点 PI ── */
static float run_pi_f32(float ref, float fb, float kp, float ki, float dt,
                         float *integral, float lo, float hi)
{
    if (!integral || !isfinite(ref) || !isfinite(fb)) { if (integral) *integral = 0.0f; return 0.0f; }
    float err = ref - fb;
    float p = kp * err;
    float ic = *integral + ki * dt * err;
    ic = program_clamp_f32(ic, lo, hi);
    float out = p + ic;
    if (out > hi) { out = hi; if (err < 0.0f) *integral = ic; }
    else if (out < lo) { out = lo; if (err > 0.0f) *integral = ic; }
    else *integral = ic;
    return out;
}

/* ── PWM 控制 ── */
static void program_apply_svpwm_to_tim1(float da, float db, float dc)
{
    uint32_t per = __HAL_TIM_GET_AUTORELOAD(&htim1) + 1;
    da = program_clamp_f32(da, 0.0f, 1.0f); db = program_clamp_f32(db, 0.0f, 1.0f); dc = program_clamp_f32(dc, 0.0f, 1.0f);
    __HAL_TIM_SET_COMPARE(&htim1, TIM_CHANNEL_1, (uint32_t)(da * per));
    __HAL_TIM_SET_COMPARE(&htim1, TIM_CHANNEL_2, (uint32_t)(db * per));
    __HAL_TIM_SET_COMPARE(&htim1, TIM_CHANNEL_3, (uint32_t)(dc * per));
}

static void program_apply_svpwm_to_tim1(const foc_svpwm_duty_t *d) { if (d) program_apply_svpwm_to_tim1(d->duty_a, d->duty_b, d->duty_c); }
static void program_apply_center_duty(void) { foc_svpwm_duty_t c = {0.5f, 0.5f, 0.5f}; program_apply_svpwm_to_tim1(&c); }

static void program_start_tim1_pwm_outputs(void)
{
    if (g_pwm_started) return;
    program_apply_center_duty();
    HAL_TIM_PWM_Start(&htim1, TIM_CHANNEL_1); HAL_TIM_PWM_Start(&htim1, TIM_CHANNEL_2); HAL_TIM_PWM_Start(&htim1, TIM_CHANNEL_3);
    HAL_TIMEx_PWMN_Start(&htim1, TIM_CHANNEL_1); HAL_TIMEx_PWMN_Start(&htim1, TIM_CHANNEL_2); HAL_TIMEx_PWMN_Start(&htim1, TIM_CHANNEL_3);
    g_pwm_started = 1U;
}

static void program_set_power_stage_enable(uint8_t en)
{
    HAL_GPIO_WritePin(N_SLEEP_GPIO_Port, N_SLEEP_Pin, en ? GPIO_PIN_SET : GPIO_PIN_RESET);
    g_power_enabled = en ? 1U : 0U;
}

/* ── 编码器观测器 ── */
static void program_reset_encoder_observer(void)
{
    g_enc_last_cnt = 0U; g_enc_primed = 0U; g_enc_speed_ok = 0U;
    g_enc_prev_mech = g_enc_cont_mech = g_enc_ws_start = 0.0f;
    g_enc_ws_cnt = 0U; g_enc_speed_raw = 0.0f;
    g_motor.speed_meas_mech_rad_s = g_motor.speed_meas_elec_rad_s = 0.0f;
    g_speed_pending = 0U; g_speed_loop_dt = FAST_LOOP_DT_S * SPEED_OBSERVER_WINDOW;
    filter_lpf_f32_init(&g_speed_lpf, program_lpf_alpha_from_cutoff_hz(g_motor.speed_meas_lpf_cutoff_hz, g_speed_loop_dt), 0.0f);
}

static void program_reset_encoder_align_runtime(void)
{ g_align_cnt = 0U; g_align_sin_sum = g_align_cos_sum = 0.0f; g_align_sample_cnt = 0U; }

static void program_reset_encoder_alignment(void) { g_align_done = 0U; g_align_cnt = 0U; g_enc_offset = 0.0f; g_motor.align_done = 0U; }

static void program_update_speed_measurement(void)
{
    if (!g_ma600a.data_valid) return;
    if (g_ma600a.sample_counter == g_enc_last_cnt) return;
    float mech = program_wrap_angle_0_2pi(program_get_encoder_rotor_mech_angle_rad());
    uint32_t dc = g_ma600a.sample_counter - g_enc_last_cnt;
    if (dc == 0U) return;
    g_enc_last_cnt = g_ma600a.sample_counter;

    if (!g_enc_primed) {
        g_enc_prev_mech = mech; g_enc_cont_mech = mech;
        g_enc_ws_start = mech; g_enc_ws_cnt = 0U;
        g_enc_speed_raw = 0.0f; g_enc_primed = 1U; g_enc_speed_ok = 0U;
        g_motor.speed_meas_mech_rad_s = g_motor.speed_meas_elec_rad_s = 0.0f;
        return;
    }
    float delta = program_wrap_delta_pm_pi(mech - g_enc_prev_mech);
    if (!isfinite(delta)) { program_reset_encoder_observer(); return; }
    g_enc_prev_mech = mech;
    g_enc_cont_mech += delta;
    g_enc_ws_cnt += dc;

    if (g_enc_ws_cnt < SPEED_OBSERVER_WINDOW) return;
    float dt = FAST_LOOP_DT_S * (float)g_enc_ws_cnt;
    if (dt <= 0.0f) { program_reset_encoder_observer(); return; }
    g_enc_speed_raw = (g_enc_cont_mech - g_enc_ws_start) / dt;
    g_enc_ws_start = g_enc_cont_mech;
    g_enc_ws_cnt = 0U;
    g_speed_loop_dt = dt;
    g_speed_lpf.alpha = program_lpf_alpha_from_cutoff_hz(g_motor.speed_meas_lpf_cutoff_hz, dt);
    if (!g_enc_speed_ok) { filter_lpf_f32_init(&g_speed_lpf, g_speed_lpf.alpha, g_enc_speed_raw); g_enc_speed_ok = 1U; }
    g_motor.speed_meas_mech_rad_s = filter_lpf_f32_update(&g_speed_lpf, g_enc_speed_raw);
    if (!isfinite(g_motor.speed_meas_mech_rad_s)) { program_reset_encoder_observer(); return; }
    g_motor.speed_meas_elec_rad_s = g_motor.speed_meas_mech_rad_s * MOTOR_POLE_PAIRS;
    g_speed_pending = 1U;
}

/* ── 对齐采样 ── */
static void program_capture_encoder_alignment_sample(void)
{
    uint32_t win_start = (ALIGN_HOLD_TICKS > ALIGN_SAMPLE_WINDOW_TICKS) ? (ALIGN_HOLD_TICKS - ALIGN_SAMPLE_WINDOW_TICKS) : 0U;
    if (g_align_cnt < win_start) return;
    float theta = program_get_encoder_raw_elec_angle_rad();
    g_align_sin_sum += sinf(theta);
    g_align_cos_sum += cosf(theta);
    g_align_sample_cnt++;
}

static float program_get_encoder_alignment_angle_rad(void)
{
    if (g_align_sample_cnt == 0U) return program_get_encoder_raw_elec_angle_rad();
    return motor_params_wrap_angle_rad(atan2f(g_align_sin_sum, g_align_cos_sum));
}

/* ── 电流反馈更新 ── */
static void program_update_current_feedback_from_raw(uint16_t ia_r, uint16_t ib_r, uint16_t ic_r, float theta)
{
    if (!g_program_telemetry.current_offset_ready) {
        g_program_telemetry.ia = g_program_telemetry.ib = g_program_telemetry.ic_meas = 0.0f;
        g_program_telemetry.id = g_program_telemetry.iq = 0.0f;
        return;
    }
    float ia = program_convert_current_from_raw(ia_r, g_program_telemetry.ia_offset_raw);
    float ib = program_convert_current_from_raw(ib_r, g_program_telemetry.ib_offset_raw);
    float ic = program_convert_current_from_raw(ic_r, g_program_telemetry.ic_offset_raw);
    g_program_telemetry.ia = ia;
    g_program_telemetry.ib = ib;
    g_program_telemetry.ic_meas = ic;
    g_program_telemetry.ic = -(ia + ib);
    if (!g_power_enabled) { g_foc.i_ab.alpha = g_foc.i_ab.beta = 0.0f; g_foc.i_dq.d = g_foc.i_dq.q = 0.0f; return; }
    foc_core_clarke(ia, ib, &g_foc.i_ab);
    float st = sinf(theta), ct = cosf(theta);
    foc_core_park(&g_foc.i_ab, st, ct, &g_foc.i_dq);
    g_program_telemetry.id = g_foc.i_dq.d;
    g_program_telemetry.iq = g_foc.i_dq.q;
}

/* ── 速度环 ── */
static float program_get_voltage_limit_v(void)
{ float vb = g_program_telemetry.vbus; return 0.57735026919f * (vb > 1.0f ? vb : 1.0f); }

static void program_update_speed_loop(void)
{
    if (!g_enc_speed_ok) { g_motor.iq_ref = g_motor.uq_ref = 0.0f; g_speed_pending = 0U; return; }
    if (!g_speed_pending) return;
    g_speed_pending = 0U;
    float *integ = g_motor.current_loop_enable ? &g_motor.speed_integral_iq : &g_motor.speed_integral_uq;
    float lim = g_motor.current_loop_enable ? g_motor.iq_limit : program_get_voltage_limit_v();
    g_motor.voltage_limit = lim;
    float out = run_pi_f32(g_motor.speed_ref_elec_rad_s, g_motor.speed_meas_elec_rad_s,
                           g_motor.speed_kp, g_motor.speed_ki, g_speed_loop_dt, integ, -lim, lim);
    if (g_motor.current_loop_enable) g_motor.iq_ref = out;
    else { g_motor.ud_ref = 0.0f; g_motor.uq_ref = out; g_motor.iq_ref = 0.0f; }
}

/* ── 电流环 ── */
static void program_run_current_loop(float theta)
{
    float vlim = program_get_voltage_limit_v();
    g_motor.voltage_limit = vlim;
    g_motor.ud_ref = run_pi_f32(g_id_applied, g_program_telemetry.id, g_motor.current_kp,
                                 g_motor.current_ki, FAST_LOOP_DT_S, &g_motor.id_integral_v, -vlim, vlim);
    g_motor.uq_ref = run_pi_f32(g_iq_applied, g_program_telemetry.iq, g_motor.current_kp,
                                 g_motor.current_ki, FAST_LOOP_DT_S, &g_motor.iq_integral_v, -vlim, vlim);
    float mag = sqrtf(g_motor.ud_ref * g_motor.ud_ref + g_motor.uq_ref * g_motor.uq_ref);
    if (mag > vlim && mag > 0.0f) { float s = vlim / mag; g_motor.ud_ref *= s; g_motor.uq_ref *= s; }
    foc_core_run_voltage_open_loop(&g_foc, g_motor.ud_ref, g_motor.uq_ref, theta, g_program_telemetry.vbus);
}

/* ── 电压模式 ── */
static void program_run_voltage_mode(float theta)
{
    float vlim = program_get_voltage_limit_v(); g_motor.voltage_limit = vlim;
    float mag = sqrtf(g_motor.ud_ref * g_motor.ud_ref + g_motor.uq_ref * g_motor.uq_ref);
    if (mag > vlim && mag > 0.0f) { float s = vlim / mag; g_motor.ud_ref *= s; g_motor.uq_ref *= s; }
    foc_core_run_voltage_open_loop(&g_foc, g_motor.ud_ref, g_motor.uq_ref, theta, g_program_telemetry.vbus);
}

/* ── 快环主控 ── */
static void program_run_speed_current_control(void)
{
    uint8_t fault = (HAL_GPIO_ReadPin(N_FAULT_GPIO_Port, N_FAULT_Pin) == GPIO_PIN_RESET) ? 1U : 0U;
    g_program_telemetry.driver_fault_active = fault;

    /* debug PWM 测试模式 */
    if (g_program_debug_pwm_test.enable) {
        if (fault) { program_set_power_stage_enable(0U); foc_core_reset_output(&g_foc); program_apply_svpwm_to_tim1(&g_foc.duty); g_motor.state = MOTOR_STATE_FAULT; }
        else { program_apply_svpwm_to_tim1(g_program_debug_pwm_test.duty_a, g_program_debug_pwm_test.duty_b, g_program_debug_pwm_test.duty_c); program_set_power_stage_enable(1U); g_motor.state = MOTOR_STATE_READY; }
        return;
    }

    /* 故障或未就绪 → 停机 */
    if (fault || !g_program_telemetry.current_offset_ready || !g_program_telemetry.ma600a_angle_valid) {
        program_set_power_stage_enable(0U); foc_core_reset_output(&g_foc); program_apply_svpwm_to_tim1(&g_foc.duty);
        g_motor.state = fault ? MOTOR_STATE_FAULT : MOTOR_STATE_READY;
        g_motor.theta_open_loop = 0.0f;
        return;
    }

    /* run_request=0 → READY */
    if (!g_motor.run_request) {
        program_set_power_stage_enable(0U); foc_core_reset_output(&g_foc); program_apply_svpwm_to_tim1(&g_foc.duty);
        program_reset_encoder_alignment(); g_motor.state = MOTOR_STATE_READY; g_motor.theta_open_loop = 0.0f;
        return;
    }

    /* 对齐 */
    if (!g_align_done) {
        g_motor.state = MOTOR_STATE_ALIGN; g_motor.align_done = 0U;
        g_motor.ud_ref = ALIGN_UD_V; g_motor.uq_ref = 0.0f;
        foc_core_run_voltage_open_loop(&g_foc, g_motor.ud_ref, g_motor.uq_ref, 0.0f, g_program_telemetry.vbus);
        program_apply_svpwm_to_tim1(&g_foc.duty); program_set_power_stage_enable(1U);
        g_align_cnt++;
        program_capture_encoder_alignment_sample();
        if (g_align_cnt >= ALIGN_HOLD_TICKS) {
            g_enc_offset = motor_params_wrap_angle_rad(program_get_encoder_alignment_angle_rad());
            g_align_done = 1U; g_motor.align_done = 1U;
            program_reset_encoder_align_runtime(); program_reset_encoder_observer();
        }
        return;
    }

    /* 速度环未就绪 → 待机电流环 */
    if (g_motor.speed_loop_enable && !g_enc_speed_ok) {
        float theta = program_get_control_elec_angle_rad();
        g_motor.theta_open_loop = theta;
        g_motor.state = MOTOR_STATE_READY;
        g_motor.ud_ref = g_motor.uq_ref = 0.0f;
        if (g_motor.current_loop_enable) program_run_current_loop(theta); else program_run_voltage_mode(theta);
        program_apply_svpwm_to_tim1(&g_foc.duty); program_set_power_stage_enable(1U);
        return;
    }

    g_motor.id_ref = 0.0f;
    if (g_motor.speed_loop_enable) program_update_speed_loop();
    g_motor.state = MOTOR_STATE_CLOSED_LOOP;
    float theta_c = program_get_control_elec_angle_rad();
    g_program_telemetry.theta_elec = theta_c;
    g_motor.theta_open_loop = theta_c;
    if (g_motor.current_loop_enable) program_run_current_loop(theta_c); else program_run_voltage_mode(theta_c);
    program_apply_svpwm_to_tim1(&g_foc.duty); program_set_power_stage_enable(1U);
}

/* ── 遥测更新 ── */
static void program_update_debug_telemetry(void)
{
    g_program_telemetry.pwm_enable_cmd = g_motor.run_request;
    g_program_telemetry.power_stage_enabled = g_power_enabled;
    g_program_telemetry.control_state = (uint8_t)g_motor.state;
    g_program_telemetry.encoder_align_done = g_align_done;
    g_program_telemetry.ma600a_sample_counter = g_ma600a.sample_counter;
    g_program_telemetry.ma600a_reject_count = g_ma600a.reject_count;
    g_program_telemetry.ma600a_comm_error_count = g_ma600a.comm_error_count;
    g_program_telemetry.speed_observer_window_samples = SPEED_OBSERVER_WINDOW;
    g_program_telemetry.current_loop_enable = g_motor.current_loop_enable;
    g_program_telemetry.position_loop_enable = g_motor.position_loop_enable;
    g_program_telemetry.control_angle_open_loop_enable = g_motor.control_angle_open_loop_enable;
    g_program_telemetry.id_ref_cmd = g_motor.id_ref;
    g_program_telemetry.iq_ref_cmd = g_motor.iq_ref;
    g_program_telemetry.id_ref_applied_cmd = g_id_applied;
    g_program_telemetry.iq_ref_applied_cmd = g_iq_applied;
    g_program_telemetry.ud_ref_cmd = g_motor.ud_ref;
    g_program_telemetry.uq_ref_cmd = g_motor.uq_ref;
    g_program_telemetry.speed_loop_ready = g_enc_speed_ok;
    g_program_telemetry.speed_ref_mech_rad_s = program_rpm_to_rad_s(g_motor.speed_ref_mech_rpm);
    g_program_telemetry.speed_ref_mech_applied_rad_s = g_motor.speed_ref_mech_applied_rad_s;
    g_program_telemetry.speed_meas_raw_mech_rad_s = g_enc_speed_raw;
    g_program_telemetry.speed_meas_mech_rad_s = g_motor.speed_meas_mech_rad_s;
    g_program_telemetry.speed_meas_elec_rad_s = g_motor.speed_meas_elec_rad_s;
    g_program_telemetry.speed_loop_dt_s = g_speed_loop_dt;
    g_program_telemetry.voltage_limit_v = g_motor.voltage_limit;
    g_program_telemetry.iq_limit_a = g_motor.iq_limit;
    g_program_telemetry.encoder_elec_offset_rad = g_enc_offset;
    g_program_telemetry.duty_a = g_foc.duty.duty_a;
    g_program_telemetry.duty_b = g_foc.duty.duty_b;
    g_program_telemetry.duty_c = g_foc.duty.duty_c;
}

/* ── DWT 周期计数 ── */
static void program_init_cycle_counter(void)
{
    CoreDebug->DEMCR |= CoreDebug_DEMCR_TRCENA_Msk;
    DWT->CYCCNT = 0U;
    DWT->CTRL |= DWT_CTRL_CYCCNTENA_Msk;
    g_dwt_ready = (DWT->CTRL & DWT_CTRL_CYCCNTENA_Msk) ? 1U : 0U;
    g_fl_period_cyc = SystemCoreClock ? (uint32_t)((float)SystemCoreClock / FAST_LOOP_HZ) : 1U;
}

/* ── 启动 ADC2 DMA + TIM6 ── */
static void program_start_adc2_dma_chain(void)
{
    HAL_ADC_Start_DMA(&hadc2, (uint32_t *)g_adc2_dma_buf, ADC2_DMA_LENGTH);
    HAL_TIM_Base_Start_IT(&htim6);
}

/* ── 初始化 ── */
void program_init(void)
{
    program_set_power_stage_enable(0U);
    memset((void *)&g_program_telemetry, 0, sizeof(g_program_telemetry));
    g_program_telemetry.ia_offset_raw = DEFAULT_CURRENT_OFFSET_RAW;
    g_program_telemetry.ib_offset_raw = DEFAULT_CURRENT_OFFSET_RAW;
    g_program_telemetry.ic_offset_raw = DEFAULT_CURRENT_OFFSET_RAW;
    g_program_telemetry.vbus = DEFAULT_VBUS_V;
    g_program_debug_pwm_test.enable = 0U;
    g_program_debug_pwm_test.duty_a = 0.30f; g_program_debug_pwm_test.duty_b = 0.40f; g_program_debug_pwm_test.duty_c = 0.60f;

    HAL_ADCEx_Calibration_Start(&hadc1, ADC_SINGLE_ENDED);
    HAL_ADCEx_Calibration_Start(&hadc2, ADC_SINGLE_ENDED);
    foc_core_init(&g_foc);
    foc_core_set_bus_voltage(&g_foc, DEFAULT_VBUS_V);
    motor_state_init(&g_motor);
    g_motor.state = MOTOR_STATE_READY;
    g_motor.run_request = 0U;
    g_motor.speed_loop_enable = 1U;
    g_motor.current_loop_enable = 1U;
    g_motor.position_loop_enable = 0U;
    g_motor.control_angle_open_loop_enable = 0U;
    g_motor.speed_kp = 0.0015f; g_motor.speed_ki = 0.015f;
    g_motor.speed_meas_lpf_cutoff_hz = SPEED_MEAS_LPF_CUTOFF_HZ;
    g_motor.iq_limit = 12.0f;
    g_motor.current_kp = 5.1522f; g_motor.current_ki = 9110.62f;
    g_motor.speed_ref_mech_rpm = 100.0f;
    g_motor.speed_ref_mech_rad_s = program_rpm_to_rad_s(100.0f);
    filter_lpf_f32_init(&g_vbus_lpf, 0.1f, DEFAULT_VBUS_V);
    filter_lpf_f32_init(&g_speed_lpf, program_lpf_alpha_from_cutoff_hz(SPEED_MEAS_LPF_CUTOFF_HZ, g_speed_loop_dt), 0.0f);
    ma600a_init(&g_ma600a, &hspi1, ENC_CS_GPIO_Port, ENC_CS_Pin);
    program_init_cycle_counter();
    ma600a_read_angle(&g_ma600a);
    program_start_tim1_pwm_outputs();
    program_start_adc2_dma_chain();
    HAL_ADCEx_InjectedStart_IT(&hadc1);
}

/* ── 后台任务 ── */
void program_task(void)
{
    uint32_t now = g_tim6_tick_ms;
    if (now == g_last_slow_ms) return;
    g_last_slow_ms = now;

    /* 读 ADC2 DMA */
    uint16_t vbus_raw = g_adc2_dma_buf[0];
    uint16_t ntc_raw  = g_adc2_dma_buf[1];
    g_program_telemetry.vbus_raw = vbus_raw;
    g_program_telemetry.ntc_raw = ntc_raw;
    g_program_telemetry.vbus = filter_lpf_f32_update(&g_vbus_lpf, program_convert_vbus_from_raw(vbus_raw));
    foc_core_set_bus_voltage(&g_foc, g_program_telemetry.vbus);

    /* 编码器读数 */
    if (g_ma600a.data_valid) {
        g_program_telemetry.ma600a_angle_raw = g_ma600a.angle_raw;
        g_program_telemetry.ma600a_angle_deg = g_ma600a.angle_deg;
        g_program_telemetry.ma600a_angle_rad = g_ma600a.angle_rad;
        g_program_telemetry.ma600a_angle_valid = g_ma600a.data_valid;
        g_program_telemetry.ma600a_consecutive_bad_count = g_ma600a.consecutive_bad_count;
        program_update_speed_measurement();
        if (g_align_done) g_program_telemetry.theta_elec = program_get_encoder_aligned_elec_angle_rad();
        else g_program_telemetry.theta_elec = program_get_encoder_raw_elec_angle_rad();
    } else {
        g_program_telemetry.ma600a_angle_valid = 0U;
        program_reset_encoder_observer(); program_reset_encoder_alignment();
    }

    g_program_telemetry.driver_fault_active =
        (HAL_GPIO_ReadPin(N_FAULT_GPIO_Port, N_FAULT_Pin) == GPIO_PIN_RESET) ? 1U : 0U;
    program_update_debug_telemetry();
}

/* ── 注入组 ADC 完成回调（10kHz 快环） ── */
void program_adc_injected_conv_cplt_callback(ADC_HandleTypeDef *hadc)
{
    if (!hadc || hadc->Instance != ADC1) return;
    uint32_t t0 = g_dwt_ready ? DWT->CYCCNT : 0U;

    uint16_t ia_r = (uint16_t)HAL_ADCEx_InjectedGetValue(hadc, ADC_INJECTED_RANK_1);
    uint16_t ib_r = (uint16_t)HAL_ADCEx_InjectedGetValue(hadc, ADC_INJECTED_RANK_2);
    uint16_t ic_r = (uint16_t)HAL_ADCEx_InjectedGetValue(hadc, ADC_INJECTED_RANK_3);
    g_program_telemetry.ia_raw = ia_r;
    g_program_telemetry.ib_raw = ib_r;
    g_program_telemetry.ic_raw = ic_r;

    /* 零偏校准 */
    if (!g_program_telemetry.current_offset_ready) {
        g_ia_offs_sum += ia_r; g_ib_offs_sum += ib_r; g_ic_offs_sum += ic_r;
        g_program_telemetry.current_offset_sample_count++;
        if (g_program_telemetry.current_offset_sample_count >= CURRENT_OFFSET_TARGET_SAMPLES) {
            g_program_telemetry.ia_offset_raw = (uint16_t)(g_ia_offs_sum / CURRENT_OFFSET_TARGET_SAMPLES);
            g_program_telemetry.ib_offset_raw = (uint16_t)(g_ib_offs_sum / CURRENT_OFFSET_TARGET_SAMPLES);
            g_program_telemetry.ic_offset_raw = (uint16_t)(g_ic_offs_sum / CURRENT_OFFSET_TARGET_SAMPLES);
            g_program_telemetry.current_offset_ready = 1U;
        }
    }

    /* 编码器读取调度 */
    ma600a_read_angle(&g_ma600a);

    /* 电流反馈 */
    float theta_fb = g_align_done ? program_get_control_elec_angle_rad() : 0.0f;
    program_update_current_feedback_from_raw(ia_r, ib_r, ic_r, theta_fb);

    /* 快环控制 */
    program_run_speed_current_control();

    if (g_dwt_ready) {
        uint32_t cyc = DWT->CYCCNT - t0;
        g_program_telemetry.fast_loop_cycles = cyc;
        if (cyc > g_program_telemetry.fast_loop_cycles_max) g_program_telemetry.fast_loop_cycles_max = cyc;
        if (cyc > g_fl_period_cyc) { g_program_telemetry.fast_loop_overrun = 1U; g_program_telemetry.fast_loop_overrun_count++; }
        else g_program_telemetry.fast_loop_overrun = 0U;
    }
}

/* ── HAL 回调转发 ── */
void program_tim_period_elapsed_callback(TIM_HandleTypeDef *htim)
{ if (htim && htim->Instance == TIM6) g_tim6_tick_ms++; }

void program_adc_conv_cplt_callback(ADC_HandleTypeDef *hadc) { (void)hadc; }

void HAL_TIM_PeriodElapsedCallback(TIM_HandleTypeDef *htim)
{ program_tim_period_elapsed_callback(htim); }

void HAL_ADC_ConvCpltCallback(ADC_HandleTypeDef *hadc)
{ program_adc_conv_cplt_callback(hadc); }

void HAL_ADCEx_InjectedConvCpltCallback(ADC_HandleTypeDef *hadc)
{ program_adc_injected_conv_cplt_callback(hadc); }

void HAL_SPI_TxRxCpltCallback(SPI_HandleTypeDef *hspi)
{ ma600a_spi_txrx_cplt_callback(&g_ma600a, hspi); }

void HAL_SPI_ErrorCallback(SPI_HandleTypeDef *hspi)
{ ma600a_spi_error_callback(&g_ma600a, hspi); }

const volatile program_telemetry_t *program_get_telemetry(void) { return &g_program_telemetry; }
motor_state_t *program_get_motor(void) { return &g_motor; }
foc_core_t *program_get_foc(void) { return &g_foc; }
```

---

## main.c 对接

在 CubeMX 生成的 `main.c` 中做以下修改：

```c
/* USER CODE BEGIN Includes */
#include "program.h"
/* USER CODE END Includes */

/* main() 中：*/
int main(void)
{
    HAL_Init();
    SystemClock_Config();

    /* ?? 初始化顺序：DMA 必须在 ADC 之前 */
    MX_GPIO_Init();
    MX_DMA_Init();
    MX_ADC1_Init();
    MX_ADC2_Init();
    MX_TIM1_Init();
    MX_TIM6_Init();
    MX_SPI1_Init();
    MX_USART1_UART_Init();   /* 可选 */
    MX_FDCAN1_Init();        /* 可选 */

    /* USER CODE BEGIN 2 */
    program_init();
    /* USER CODE END 2 */

    while (1)
    {
        /* USER CODE BEGIN 3 */
        program_task();
    }
}
```

---

## 完整编译需要加入 Keil 的源文件清单

| 分组 | 文件 |
|------|------|
| **App/** | `program.c`, `foc_core.c`, `motor_state.c`, `drv_pid.c`, `filter.c`, `ma600a.c` |
| **Core/Src/** | `main.c`, `adc.c`, `tim.c`, `spi.c`, `dma.c`, `gpio.c`, `usart.c`, `stm32g4xx_it.c` |
| **Include Paths** | `App/`, `Core/Inc/`, `Drivers/CMSIS/Include/` 等 |

> 按照 ①~⑧ 的顺序逐个创建文件，全部完成后编译下载即可运行完整 FOC 程序。
>
> 首次上电：功率级默认休眠 → ADC 校准 → 零偏采集(约100ms) → 编码器采数 → 等待 `g_motor.run_request = 1` 启动。















////////////////// 补上

---

## 第8步：闭环 FOC 控制

**前置条件**：第 7 步开环拖动已验证电机能转。

### 8.1 速度测量（基于编码器差分）

**文件**：`program_current.c`

```c
// 速度观测内部状态 → 放在 program_update_speed_measurement 函数上方
#define SPEED_OBSERVER_WINDOW  20U
static float           g_prev_mech_angle    = 0.0f;  // 上一次机械角
static float           g_continuous_mech_rad = 0.0f;  // 连续累加（无跳变）
static uint32_t        g_speed_window_count = 0;
static float           g_speed_window_start = 0.0f;
static volatile float  g_speed_meas_mech    = 0.0f;  // 机械角速度 rad/s
static uint8_t         g_speed_ready        = 0;

static void program_update_speed_measurement(float mech_angle)
{
    if (!g_speed_ready) {
        g_prev_mech_angle    = mech_angle;
        g_continuous_mech_rad = mech_angle;
        g_speed_window_start = mech_angle;
        g_speed_window_count = 0;
        g_speed_ready = 1;
        return;
    }
    float delta = program_wrap_delta_pm_pi(mech_angle - g_prev_mech_angle);
    g_prev_mech_angle = mech_angle;
    g_continuous_mech_rad += delta;
    g_speed_window_count++;
    if (g_speed_window_count >= SPEED_OBSERVER_WINDOW) {
        float dt = 0.0001f * (float)g_speed_window_count;
        g_speed_meas_mech = (g_continuous_mech_rad - g_speed_window_start) / dt;
        g_speed_window_start = g_continuous_mech_rad;
        g_speed_window_count = 0;
    }
}
```

**调用**：在 `program_current_fast_loop()` 中 `ma600a_read_angle` 之后调用：
```c
program_update_speed_measurement(g_ma600a.angle_rad);
```

> **验证点**：调试器观察 `g_speed_meas_mech`，手转电机轴应显示对应 rad/s。

### 8.2 电流换算

```c
// ADC 原始值 → 相电流(A)
static float convert_current(uint16_t raw, uint16_t offset)
{
    return ((float)raw - (float)offset) * 3.3f / 4095.0f / (20.0f * 0.01f);
}
// 解释：(码值 ? 零偏) × 3.3V/4095 / (INA240增益20 × 采样电阻0.01Ω)
```

### 8.3 带抗饱和的 PI 控制器

**文件**：`program_current.c`，放在 `program_current_fast_loop` 上方。

```c
// ── 电流/速度环 PI 参数 ──
/** 电流环 PI 参数：比例增益（带宽 1kHz） */
#define CURRENT_KP       5.15f
/** 电流环 PI 参数：积分增益 */
#define CURRENT_KI    9110.6f
/** 速度环 PI 参数：比例增益 */
#define SPEED_KP        0.0015f
/** 速度环 PI 参数：积分增益 */
#define SPEED_KI        0.015f
/** q 轴电流限幅 (A) */
#define IQ_LIMIT_A      12.0f

/** d 轴电流 PI 积分项 */
static float g_id_integral = 0.0f;
/** q 轴电流 PI 积分项 */
static float g_iq_integral = 0.0f;
/** 速度环 PI 积分项 */
static float g_speed_integral = 0.0f;

/**
 * 带抗饱和的浮点 PI 控制器
 * @param ref       目标值
 * @param fb        反馈值
 * @param kp        比例增益
 * @param ki        积分增益
 * @param dt        控制周期 (s)
 * @param integral  积分项指针（跨调用保持）
 * @param lo        输出下限
 * @param hi        输出上限
 * @return 限幅后的 PI 输出
 */
static float run_pi_f32(float ref, float fb, float kp, float ki,
                         float dt, float *integral, float lo, float hi)
{
    /** 计算误差 */
    float error = ref - fb;
    /** 比例项 */
    float p_out = kp * error;
    /** 积分候选值 = 上次积分 + ki × dt × error */
    float i_cand = *integral + ki * dt * error;
    /** 积分项限幅 */
    i_cand = program_clamp_f32(i_cand, lo, hi);
    /** 总输出 = P + I */
    float out = p_out + i_cand;
    /** 抗饱和：仅在不加深饱和的方向更新积分 */
    if (out > hi)      { out = hi; if (error < 0.0f) *integral = i_cand; }
    else if (out < lo) { out = lo; if (error > 0.0f) *integral = i_cand; }
    else              { *integral = i_cand; }
    return out;
}
```

### 8.4 闭环 FOC 主逻辑（替换开环拖动）

**文件**：`program_current.c` 的 `program_current_fast_loop()`，在对齐完成后（`g_align_done == 1`）替换开环拖动代码：

```c
if (g_align_done) {
    // ① 电流换算
    float ia = convert_current(ia_raw, g_ia_offset);
    float ib = convert_current(ib_raw, g_ib_offset);

    // ② Clarke：ia/ib → αβ（ic = -ia-ib 自动满足）
    float i_alpha = ia;
    float i_beta  = (ia + 2.0f * ib) / 1.73205080757f;

    // ③ 获取电角度
    float mech_angle = g_ma600a.angle_rad;
    float elec_angle = program_wrap_angle_0_2pi(mech_angle * 14.0f - g_elec_offset_rad);

    // ④ Park：αβ → dq
    float sin_t = sinf(elec_angle);
    float cos_t = cosf(elec_angle);
    float id =  i_alpha * cos_t + i_beta * sin_t;
    float iq = -i_alpha * sin_t + i_beta * cos_t;

    // ⑤ 速度环 PI（200Hz 级联）→ iq_ref
    float iq_ref = 0.0f;
    if (g_speed_ready && (g_speed_window_count == 0)) {
        float speed_dt = 0.0001f * SPEED_OBSERVER_WINDOW;
        iq_ref = run_pi_f32(g_speed_ref, g_speed_meas_mech,
                            SPEED_KP, SPEED_KI, speed_dt,
                            &g_speed_integral, -IQ_LIMIT_A, IQ_LIMIT_A);
    }

    // ⑥ 母线电压 & 限幅
    float vbus = program_clamp_f32(
        (float)g_adc2_dma_buf[0] * 3.3f / 4095.0f * 25.0f, 1.0f, 100.0f);
    float v_limit = vbus * 0.577f;   // vbus/√3

    // ⑦ 电流环 PI：id→0, iq→iq_ref
    float ud = run_pi_f32(0.0f, id, CURRENT_KP, CURRENT_KI, 0.0001f,
                          &g_id_integral, -v_limit, v_limit);
    float uq = run_pi_f32(iq_ref, iq, CURRENT_KP, CURRENT_KI, 0.0001f,
                          &g_iq_integral, -v_limit, v_limit);

    // ⑧ 电压矢量限幅
    float v_mag = sqrtf(ud * ud + uq * uq);
    if (v_mag > v_limit && v_mag > 0.0f) {
        float scale = v_limit / v_mag;
        ud *= scale; uq *= scale;
    }

    // ⑨ 反Park + SVPWM（复用 foc_core）
    foc_core_run_voltage_open_loop(&g_foc, ud, uq, elec_angle, vbus);
    program_apply_svpwm_to_tim1(&g_foc.duty);
    HAL_GPIO_WritePin(N_SLEEP_GPIO_Port, N_SLEEP_Pin, GPIO_PIN_SET);
}
```

> **验证点**：调试器设 `g_speed_ref = 100`（rad/s），电机闭环稳速运行。`id ≈ 0`，`iq` 随负载变化。

### 8.5 测试

| # | 测试项 | 方法 | 通过标准 |
|---|--------|------|---------|
| 8.1 | 速度测量 | 手转电机轴，看 `g_speed_meas_mech` | 值随转速变化 |
| 8.2 | id 趋零 | 空载运行时看 `id` | `id ≈ 0`（±0.5A 内） |
| 8.3 | 速度跟踪 | 改 `g_speed_ref` 50→100→200 | 实际转速跟随 |
| 8.4 | 负载扰动 | 手捏电机轴后松开 | 转速跌落后恢复 |
| 8.5 | 正反转 | `g_speed_ref` 设负值 | 平滑切换 |
| 8.6 | 稳态电流 | 空载观察 `iq` | < 2A |
| 8.7 | 长时间运行 | 运行 5 分钟 | 无过热异响 |

---

## 第9步：状态机驱动

**目标**：用电控标准六状态自动管理启停、对齐、开环、闭环、故障。

**文件**：新建 `App/motor_state.h` + `App/motor_state.c`（完整代码见上文 ⑤ motor_state.h/c）。

### 9.1 状态流程

```
INIT ──→ READY ──run_request=1──→ ALIGN(锁转子800ms)
  ↑                                  │
  │                            ┌─────┘
  │                            ▼
  │                    OPEN_LOOP(开环拖动1.5s)
  │                            │
  │                      run_request=0  │  1.5s 后
  │         ?──────────────────────────┘
  │                            ▼
  │                    CLOSED_LOOP(闭环运行)
  │                            │
  └────run_request=0────────────────┘

  任意状态 ──nFAULT/PB15=0──→ FAULT(停机,等待清除)
```

### 9.2 集成

在 `program.h` 中声明：
```c
extern motor_state_t g_motor;
```

在 `program_current_fast_loop()` 开头检查状态：
```c
if (g_motor.state == MOTOR_STATE_FAULT) {
    program_apply_svpwm_to_tim1(&g_foc.duty);  // 50% 占空比
    HAL_GPIO_WritePin(N_SLEEP_GPIO_Port, N_SLEEP_Pin, GPIO_PIN_RESET);  // 休眠
    return;
}
```

**启动命令**：`g_motor.run_request = 1`。

### 9.3 测试

| # | 测试项 | 方法 | 通过标准 |
|---|--------|------|---------|
| 9.1 | 初始状态 | 上电后读 `g_motor.state` | = MOTOR_STATE_READY |
| 9.2 | 启动流程 | `run_request = 1` | 依次经过 ALIGN→OPEN_LOOP→CLOSED_LOOP |
| 9.3 | 对齐锁定 | ALIGN 期间试拧电机轴 | 有明显阻力 |
| 9.4 | 正常停机 | `run_request = 0` | PWM 回到 50%，PB14 变低 |
| 9.5 | 状态切换 | 反复启停 5 次 | 无异常 |

---

## 第10步：异常与故障保护

### 10.1 DRV 芯片 nFAULT 检测

```c
// 在 program_current_fast_loop 开头
uint8_t fault = (HAL_GPIO_ReadPin(N_FAULT_GPIO_Port, N_FAULT_Pin) == GPIO_PIN_RESET) ? 1 : 0;
if (fault) {
    g_motor.fault_code = MOTOR_FAULT_DRIVER;
    // 立即停机
    foc_core_reset_output(&g_foc);
    program_apply_svpwm_to_tim1(&g_foc.duty);
    HAL_GPIO_WritePin(N_SLEEP_GPIO_Port, N_SLEEP_Pin, GPIO_PIN_RESET);
    return;
}
```

### 10.2 过压保护

```c
// 在 program_task() 1kHz 慢环中
if (g_program_telemetry.vbus > 36.0f) {  // 阈值根据额定电压调整
    g_motor.fault_code = MOTOR_FAULT_OVERVOLTAGE;
    g_motor.run_request = 0;
}
```

### 10.3 快环超时保护

```c
// 在回调入口和出口打时间戳
void HAL_ADCEx_InjectedConvCpltCallback(ADC_HandleTypeDef *hadc)
{
    if (hadc->Instance != ADC1) return;
    uint32_t t0 = DWT->CYCCNT;  // 需要先初始化 DWT
    // ... 快环处理 ...
    uint32_t elapsed = DWT->CYCCNT - t0;
    if (elapsed > (SystemCoreClock / 10000)) {  // 超过 100?s
        g_program_telemetry.fast_loop_overrun = 1;
        g_program_telemetry.fast_loop_overrun_count++;
    }
}
```

### 10.4 测试

| # | 测试项 | 方法 | 通过标准 |
|---|--------|------|---------|
| 10.1 | nFAULT 响应 | 短接 PB15 到 GND | 电机立即停转，PB14 变低 |
| 10.2 | 过压保护 | 调高电源电压超过阈值 | 自动停机 |
| 10.3 | 故障恢复 | 清除故障后 `run_request=1` | 重新正常启动 |
| 10.4 | 快环不超时 | 观察 `fast_loop_overrun` | = 0 |

---

## 完整功能对照表

| 功能 | 代码位置 | 测试见 |
|------|---------|--------|
| 电流环 PI | `program_current.c` 8.4 | 8.5 测试 |
| 速度环 PI | `program_current.c` 8.4 | 8.5 测试 |
| 位置环 | `program.c` 补充5 | 补充5.8 |
| 状态机 | `motor_state.c` ⑤ | 9.3 测试 |
| nFAULT 保护 | `program_current.c` 10.1 | 10.4 测试 |
| 过压保护 | `program.c` program_task | 10.4 测试 |
| 快环超时 | `program_current.c` 10.3 | 10.4 测试 |
| 电压限幅 | `program_current.c` 8.4⑧ | 空载+满载验证 |
| 电流/速度斜坡 | `program.c` 补充2 | 切换观察平滑性 |

---

> **学习路径**：第1步→第8步→第9步→第10步→补充(位置环等高级功能)，每步独立验证通过后再进入下一步。全部完成后你将掌握 FOC 从硬件驱动到三环控制的完整链路。

---

## 重要补充说明（复刻必读）

### 主运行入口澄清

实际控制流程：
```
ADC1 硬件触发(10kHz)
  → HAL_ADCEx_InjectedConvCpltCallback()      [program.c 回调]
  → program_adc_injected_conv_cplt_callback() [薄封装]
  → 读 ADC + 零偏校准 + 编码器 + 控制逻辑    [快环主逻辑]
  → program_apply_svpwm_to_tim1()             [下发占空比]
```

**motor_state.c 的状态机当前未被主流程调用**。`motor_state_task()` 是独立状态机框架，未来集成时可在 10kHz 快环中检查 `g_motor.run_request` 来驱动状态流转。当前项目通过 `g_align_done` 标志手动切换对齐→开环→闭环。

### 1kHz program_task() 不是控制环

`program_task()` 只做：
- 读取 ADC2 DMA 缓冲（vbus/ntc 原始值）
- 更新遥测结构体
- 故障状态刷新

**所有实时控制（电流/速度/位置）均在 10kHz 注入 ADC 回调中完成**。

### FDCAN 预留（未启用）

`MX_FDCAN1_Init()` 已初始化硬件，`fdcan.c` 在工程中，但 **App/ 层没有任何 CAN 收发逻辑**。若要接入 CANopen，需自行实现。

### 几个隐藏但关键的控制细节

| 细节 | 说明 | 参考位置 |
|------|------|---------|
| 零偏 1024 次校准 | 每次注入完成累加，满 1024 次取均值作为 offset | Step 5 |
| 对齐 512 点 sin/cos 平均 | 对齐后期取 512 次电角的正弦/余弦累加，用 atan2 求平均方向，抗噪声 | program.c align_sample |
| 编码器连续角重归一化 | 连续角超过 32 圈时减去整数圈，防止浮点精度丢失 | 补充4 |
| 低速量化保护 | 转速低于编码器分辨率 ×8 时强制归零，抑制量化噪声 | 补充3 |
| 位置环 hold/release/creep | 到位后滞回保持、超阈值确认退出、小误差蠕动 | 补充5 |
| DWT 快环耗时 | 用 DWT->CYCCNT 测每拍耗时，超 100μs 标 overrun | program.c 10.3 |

### NTC 温度未换算

ADC2 读取了 `ntc_raw`（PB11 原始码值），但**没有转换成实际温度（℃）**。若需温度保护/显示，需自行添加 NTC 分压公式或查表。

### 调试串口 VOFA 协议

参考项目用 DMA 发送 + busy 标志位 + JustFloat 尾帧实现高速波形显示，不是简单 printf。详见 `cli_uart.c/h`。

---

> **复刻检查清单**：理解主运行入口 → 区分 10kHz/1kHz 职责 → FDCAN标记预留 → 掌握隐藏细节 → NTC 仅原始值 → 串口用 VOFA 协议。

---

## 快环与慢环架构

### 概述

项目采用**双速率架构**，将实时控制与监控辅助分离到两个不同频率的执行上下文中：

| | **快环 (Fast Loop)** | **慢环 (Slow Loop)** |
|---|---|---|
| **频率** | 10 kHz | 1 kHz |
| **周期** | 100 μs | 1 ms |
| **触发源** | ADC1 注入组转换完成中断（硬件触发，与 PWM 同步） | TIM6 定时器中断 |
| **入口函数** | `HAL_ADCEx_InjectedConvCpltCallback` → `program_current_fast_loop()` | `program_Task()` |
| **核心职责** | **实时控制**：FOC、PI、SVPWM | **监控辅助**：VBUS、NTC、遥测 |

### 快环执行流程（10kHz）

```
ADC1 注入转换完成（硬件触发，与 PWM 同步）
    │
    ▼
HAL_ADCEx_InjectedConvCpltCallback()    ← 在 program.c
    │  读取 ia_raw, ib_raw, ic_raw
    ▼
program_current_fast_loop()             ← 在 program_current.c
    │
    ├── ① 零偏校准（仅启动时，采集 1024 次取均值）
    ├── ② ADC 原始值 → 相电流 (A)
    ├── ③ 读取 MA600A 编码器角度
    ├── ④ 速度观测（每 20 拍 = 2ms 输出一次平均速度）
    ├── ⑤ 转子对齐（启动时，Ud 锁轴 + sin/cos 平均求偏置）
    ├── ⑥ 电流/速度斜坡（避免阶跃冲击）
    ├── ⑦ PI 控制器（电流环 + 速度环级联）
    └── ⑧ 调用 program_svpwm（开环或闭环 SVPWM 输出）
            │
            ├── 开环：积分器生成电角度 → 反Park → SVPWM → TIM1
            └── 闭环：Clarke → Park → PI → 反Park → SVPWM → TIM1
```

### 慢环执行流程（1kHz）

```
TIM6 定时器中断（每 1ms）
    │
    ▼
g_program_tick_ms 累加
    │
    ▼
program_Task()                           ← 在 program.c
    │
    ├── ① 读取 ADC2 DMA 缓冲：[0]=VBUS, [1]=NTC
    ├── ② VBUS 码值 → 母线电压 (V)
    ├── ③ NTC 码值（仅原始值，未换算温度）
    ├── ④ 读取编码器角度（遥测用）
    ├── ⑤ 调试 PWM 输出控制
    ├── ⑥ VOFA 遥测数据发送
    └── ⑦ 故障状态刷新
```

### 为什么这样设计

1. **快环由硬件触发**：ADC1 注入组由 TIM1 的 TRGO 触发，与 PWM 载波同步，保证电流采样时刻一致（避开开关噪声），这是 FOC 的基本要求。

2. **快环不做非控制事务**：10kHz 下每拍只有 100μs，必须严格控制耗时。VBUS 采样、遥测发送等非实时任务放到 1kHz 慢环，避免快环超时（overrun）。

3. **慢环不参与控制**：`program_Task()` 只做监控和辅助，**绝不**执行电流/速度/位置环计算。所有控制逻辑均通过快环的中断回调链完成。

4. **数据流向**：快环**写入**遥测结构体（`g_program_telemetry`），慢环**读取**并发送，形成生产者-消费者模式。

### 关键宏定义

```c
#define FAST_LOOP_DT_S  0.0001f   /* 快环周期 100μs = 10kHz */
```

`FAST_LOOP_DT_S` 是快环的时间基准，用于：
- 电流斜坡：`current_step = 150A/s × 0.0001s = 0.015A/拍`
- 速度斜坡：`speed_step = 100rad/s² × 0.0001s = 0.01rad/s/拍`
- 开环角度积分：`theta += speed × 0.0001`
- PI 控制器的积分项累加

### 相关文件

| 文件 | 角色 |
|------|------|
| `Core/App/program.c` | 主入口：`HAL_ADCEx_InjectedConvCpltCallback`（快环入口）、`program_Task`（慢环） |
| `Core/App/program_current.c` | 快环核心：`program_current_fast_loop()` — 电流采样、速度观测、对齐 |
| `Core/App/program_svpwm.c` | SVPWM 输出层：开环/闭环 SVPWM、电流/速度斜坡函数 |
| `Core/Inc/main.h` | 全局宏定义（`FAST_LOOP_DT_S` 等） |
| `Core/Src/tim.c` | TIM1 PWM 配置（触发 ADC1 注入）、TIM6 1ms 时基 |
| `Core/Src/adc.c` | ADC1 注入组 + ADC2 DMA 配置 |